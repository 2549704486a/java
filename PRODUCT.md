# 积分激励与智能运营产品基线

> 本页描述用户和运营人员当前能够完成什么，以及产品明确不做什么。实现细节和开发顺序分别查看 `docs/agent-context/` 与路线图；源码与本页冲突时，以当前源码为准并修正文档。

## 1. Product

这是一个围绕“获得积分、规划积分、兑换奖品、运营增长活动”形成闭环的积分激励产品。

它不是单纯的聊天 Demo。用户侧 Agent 帮助用户理解实时资格、制定可执行计划并安全发起兑换；运营侧 Agent 帮助运营人员读取事实、形成活动草案和解释效果，但不能绕过审核工作流直接改变线上规则。

## 2. Users

| 角色 | 核心诉求 |
| --- | --- |
| 普通用户 | 了解积分与任务、寻找合适奖品、制定积分计划、安全兑换并查看最终结果 |
| 活动策划人员 | 基于当前奖品、任务、客群和历史信息生成可编辑的活动草案 |
| 审核与发布人员 | 检查草案约束，在权限范围内审核、拒绝或发布活动 |
| 效果分析人员 | 查看活动触达、阅读、点击、参与和兑换漏斗，区分实验效果与财务收益 |

运营角色由权限区分，不假设同一个人天然拥有草案、审核和发布的全部权限。

## 3. Jobs To Be Done

### 用户侧

- 当用户想兑换奖品时，基于实时积分、奖品条件和可用任务说明“现在能否兑换”和“还差多少”。
- 当用户有长期目标或稳定偏好时，在后续会话中按需使用这些信息，同时不覆盖实时业务事实。
- 当用户明确决定兑换时，通过受控确认发起旧事务消息链路，并引导用户从订单页面查看最终状态。
- 当用户收到活动消息时，可以在产品内阅读、点击并参与活动。

### 运营侧

- 当运营人员提出业务问题时，先回答其真实问题，再在必要时说明能力或数据边界。
- 当运营人员规划活动时，读取当前事实和规则，生成结构化、可编辑、可追溯的草案。
- 当草案进入执行阶段时，通过确定性状态机完成提交、审核、发布、客群快照、实验分组和站内信投放。
- 当运营人员复盘活动时，自动发现内部活动，不要求其记忆活动 ID，并解释漏斗、Lift、样本与数据来源限制。

## 4. Core User Journeys

### 4.1 用户规划与兑换

```text
可信用户身份
  -> 查询实时积分、奖品、任务或知识
  -> 生成积分计划或奖品建议
  -> 准备兑换并展示关键条件
  -> 用户明确确认
  -> 确定性服务发起旧事务消息链路
  -> 订单页面展示处理中、成功或失败
```

“已受理”不等于“兑换成功”。最终状态来自异步业务链路，不能由模型根据对话自行判断。

### 4.2 运营活动闭环

```text
运营身份与权限
  -> 查询客群、奖品、任务、规则和历史活动
  -> Agent 生成活动草案与风险说明
  -> 人工提交、审核或拒绝
  -> 审核通过后由确定性服务发布
  -> 固定客群快照与实验分组
  -> 站内信投放与行为事件回流
  -> 查看活动漏斗和 Lift
```

本地演示行为可以验证这条链路，但不能替代真实生产用户行为。

## 5. Product Principles

1. **先解决问题，再声明边界。** 咨询问题应先给出有用答案，不能用大段免责说明替代回答。
2. **模型负责理解和表达，确定性服务负责事实与写操作。** 可计算规则、状态迁移、权限和幂等不依赖模型临场发挥。
3. **系统能发现的内部参数不向用户索取。** 活动 ID、用户 ID 等内部标识应由身份或查询能力获得。
4. **实时事实与知识解释分离。** 积分、库存、订单和活动状态走业务接口；制度、说明和历史案例可以走 RAG。
5. **高风险动作受控。** 兑换、审核和发布必须经过可信身份、权限、确认或状态机以及审计。
6. **数据口径透明。** 明确区分真实、可计算、Fixture、Simulated、计划和假设。
7. **复杂技术由业务证据触发。** 不为了展示 MCP、多 Agent、复杂改写或重排而增加系统复杂度。

## 6. Current Product Baseline

| 能力 | 当前状态 | 边界 |
| --- | --- | --- |
| 用户积分规划与奖品推荐 | 已实现 | 使用实时业务数据，不由模型自行计算 |
| 长期目标与偏好 | 已实现 | 保存用户明确表达的稳定信息，不保存动态业务事实 |
| 规则知识问答与引用 | 已实现 | 受众、时效和冲突规则受控；无可靠知识时拒答 |
| 受控兑换与订单查询 | 已实现 | 只走旧事务消息链路，确认受理后仍以订单最终状态为准 |
| 用户站内信阅读与点击 | 已实现 | 当前产品渠道为站内信，不等同于短信、邮件或外部推送 |
| 运营活动草案与工作台 | 已实现 | Agent 生成建议，不能直接发布 |
| 审核、发布与效果回流 | 已实现 | 由带权限的确定性工作流执行 |
| 活动客群、实验分组与漏斗 | 已实现本地闭环 | 本地活跃摘要和演示效果包含 Fixture/Simulated 数据 |
| 任意自定义指标计算 | 未实现 | 只支持已注册且证据口径完整的能力 |
| MCP | 未落地 | 既有实现已回滚，等待多个真实调用方和复用收益 |
| 多 Agent | 未落地 | 等待单 Agent 暴露稳定职责冲突后再比较收益 |

## 7. Metrics And Evidence

| 类别 | 当前可以说明什么 | 不能说明什么 |
| --- | --- | --- |
| 业务事实 | 积分、任务、奖品、兑换订单和活动工作流状态 | 不能用知识库或记忆替代实时查询 |
| 活动过程指标 | 在已支持口径内统计触达、阅读、点击、参与和兑换漏斗 | 未注册的任意交集指标不能由模型拼接聚合值后宣称已计算 |
| 实验指标 | 对固定实验组与对照组计算当前活动的 Lift | 小样本或模拟行为不能证明生产收益 |
| 财务收益 | 当前可以展示有明确事实来源的成本和业务金额 | 不能把积分直接换算为现金，也不能把运营 Lift 称为财务收益 |
| 产品目标 | 提高任务参与、兑换完成和活动运营效率 | 在接入真实生产行为前，这些仍是待验证目标而非既成成果 |

## 8. Explicit Non-Goals

- 不恢复新的兑换链路，不让 Agent 绕过旧事务消息流程。
- 不允许模型直接修改积分、库存、活动状态或执行任意数据命令。
- 不把历史压测数字作为 Agent 产品决策依据。
- 不把本地 Fixture、Simulated 数据包装成真实用户增长收益。
- 不在没有直接业务收益证据时引入 MCP、多 Agent 或通用能力平台。
- 不承诺回答系统尚未注册、缺少完整口径和事实来源的任意分析问题。

## 9. Where To Go Next

- 当前优先级：[Agent 课程能力落地路线图](docs/agent-design/20_Agent课程能力落地路线图.md)
- 项目事实：[Agent 上下文包](docs/agent-context/README.md)
- 代码与运行文档：[文档导航](docs/README.md)
- 复杂功能设计：[OpenSpec 当前变更](openspec/changes/)；使用规则见 [AGENTS.md](AGENTS.md)
