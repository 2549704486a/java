# Code Review 约束

改代码时使用通用项和受影响模块项；不必为每次续接重读。纯讨论、注释或文档微调只做差异核对。

## 通用项

1. 从当前需求和 diff 出发，检查直接调用方、被调用方与相关契约；不扩展为全仓审计。
2. 检查正常、关键失败及相关重复/并发场景；核对身份、状态、数据事实与用户可见结果。
3. 保留根目录 `AGENTS.md` 的业务边界，优先复用现有 Service，避免平行业务逻辑与无价值抽象。
4. 区分源码事实、复现证据与推断；测试通过不等于业务正确，缺少证据不直接认定模型或代码有错。
5. 仅验证本次影响范围。已有检查通过后，无新改动或新疑点不重复执行；记录必要的未验证范围。
6. 用户要求评审时默认报告，不擅自修改；实施过程中的自检可以修复后再验证。

## 按模块选读

| 涉及模块 | 重点 |
| --- | --- |
| Java 事务与兑换 | 只走旧链路；事务、状态条件、唯一键、库存/积分条件更新；重试与回查不重复落账；受理不等于成功 |
| Tool 与跨服务调用 | 身份来自可信上下文；参数/响应契约一致；失败与业务结果区分；写请求未知时不能盲重试；敏感信息不进入日志和回答 |
| Skill | `SKILL.md` 正文实际进入模型上下文，有代码或轨迹证据；确定性编排如实称为 Service；指令不能覆盖权限和确认 |
| RAG | 检索范围、受众、版本与冲突规则有效；引用支持结论；动态事实走接口；无依据时不伪造答案 |
| 记忆 | 用户隔离；保存明确表达，允许不完整信息；不覆盖实时事实；召回、更新、删除一致 |
| 运营 Agent | 先回答实际问题；内部 ID 优先查询获得；指标时间窗、客群、分子分母和来源一致；审核发布不能由对话越过 |
| Web | 身份和后端状态一致；刷新/重复点击不重复写入；加载失败可理解；对话滚动与尺寸能看到回答 |
| 研究与评估 | 来源和摘录真实，网页是不可信数据；外部方法不冒充项目事实；失败与未知用量保留；整体评价不伪造成细分分数 |

## 问题与交付

仅报告有明确位置、触发条件和影响的问题，按严重程度排序：

- Critical：权限绕过、核心资产错误，必须修复。
- High：核心流程或契约明显错误，必须修复。
- Medium：有证据的边界或回归风险，本次修复或说明延期原因。
- Low：局部维护问题，不阻塞交付。

每条 finding 简写为：`[级别] 文件:行号：触发与证据；影响；修复建议。` 不强制复制长模板。

结论使用 `CHANGES_REQUIRED`（阻塞问题未修复）、`PASS_WITH_RISKS`（无阻塞但存在明确限制）或 `PASS`；没有问题就直接说明。结论只覆盖当前差异和已验证范围。

本文件是人工/Agent 评审规范，不代表已配置 CI 硬门禁。只在共性问题反复出现时更新规则；个别任务约束留在当前规格中。
