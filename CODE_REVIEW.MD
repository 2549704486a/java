# Code Review Guide

> 本文是本仓库唯一的通用 Code Review 基线。它用于发现会影响业务行为、数据状态和 Agent 可信度的问题，不是代码格式检查清单。具体变更的额外约束写入对应的 OpenSpec change，不在这里堆积一次性规则。

## 1. 使用方式

以下场景必须执行 Code Review：

- 修改兑换、积分、库存、订单、事务消息或活动发布等核心链路；
- 修改身份、权限、确认、幂等、重试或状态机；
- 修改 Agent Tool、Skill、长期记忆、RAG 或模型输出约束；
- 修改跨 Java、Python、前端或存储的接口契约；
- 修复可能在相邻场景中复发的问题；
- 复杂需求的每个可独立交付任务完成后，以及最终集成前。

纯注释、错别字等不改变行为的改动可以只做差异检查，不必机械套用完整流程。

评审开始前，至少准备以下信息：

1. 用户真正要解决的问题；
2. 本次变更的目标、非目标和验收条件；
3. 相关 Spec、接口或数据契约；
4. 代码差异及其上下游调用链；
5. 已执行的验证和仍未验证的范围。

如果这些信息不足，应先指出缺口，不能凭文件名或测试通过推断业务一定正确。

## 2. 评审顺序

按以下优先级检查，不要先花大量篇幅讨论命名和格式：

1. **业务正确性**：实现是否真的回答了需求，正常流程和失败流程是否正确。
2. **产品边界**：是否恢复了已删除能力、虚构了未实现能力，或突破明确的非目标。
3. **状态与数据一致性**：事务、消息、缓存、数据库和状态机是否可能产生矛盾。
4. **身份与高风险动作**：可信身份、权限、人工确认、幂等和审计是否仍然有效。
5. **跨服务契约**：请求参数、响应 Schema、错误语义、超时和重试是否一致。
6. **Agent 可信度**：模型是否可能把猜测、过期知识或模拟数据当作业务事实。
7. **可观测与可恢复性**：失败后能否定位请求、判断结果并安全恢复。
8. **可维护性**：是否出现重复业务实现、职责混乱或没有业务价值的抽象。
9. **代码风格**：仅在影响理解、维护或工具检查时报告。

## 3. 严重级别

| 级别 | 定义 | 本项目示例 | 交付要求 |
| --- | --- | --- | --- |
| `Critical` | 会造成权限绕过、资产或核心数据错误，且容易在线上触发 | 越权兑换、重复扣积分、超卖后仍报告成功、绕过活动审核直接发布 | 必须修复，禁止交付 |
| `High` | 核心流程、状态或契约错误，会导致明显错误结果 | 写请求结果未知时自动重试、把处理中当成功、跨用户读取记忆、模型使用伪造库存 | 必须修复，禁止交付 |
| `Medium` | 边界场景、可观测性或后续回归风险明确，但不立即破坏核心数据 | 错误码映射丢失、缺少关键请求关联信息、RAG 冲突处理不完整 | 原则上本次修复；延期需记录理由和风险 |
| `Low` | 不改变当前行为的局部维护问题 | 重复代码、误导性命名、难以理解但暂未出错的结构 | 可改进，不阻塞交付 |

不要为了显得问题严重而抬高级别。级别由“触发条件、影响范围和后果”决定，而不是由改动文件多少决定。

## 4. 全仓库不可破坏项

以下约束是每次评审都要核对的项目基线：

- 兑换只保留旧事务消息链路，不得重新引入或假设新链路存在。
- 实时积分、库存、订单和活动状态来自业务接口，不能由模型、RAG 或长期记忆补全。
- Java 业务服务是核心业务规则的事实来源；REST、Tool 或未来其他协议入口不得复制一套平行业务逻辑。
- 模型不能直接执行任意 SQL、Redis 命令、库存修改、积分修改或跳过审核发布。
- 兑换等高风险写操作必须保留可信身份、明确确认、幂等、确定性执行和审计能力。
- 查询失败可以按明确策略重试；写操作结果未知时不得盲目重试或向用户声称失败。
- `FIXTURE`、`SIMULATED` 和本地演示结果必须显式标注，不能包装为真实用户行为或运营收益。
- 计划、文档和模型推断不能覆盖当前源码、接口契约及可复现运行证据。
- 不为尚未成立的 MCP、多 Agent 或通用平台需求提前增加抽象。

## 5. 分模块检查

### 5.1 Java 业务服务

- 事务边界是否覆盖必须原子完成的数据库操作；是否存在自调用等导致事务未生效的情况。
- 本地事务、事务消息状态与最终业务状态是否一致；异常分支能否被回查或补偿正确识别。
- 唯一键、状态条件更新和幂等逻辑是否能抵抗重复消息与重复请求。
- 库存分片选择、扣减和重试是否仍遵守旧链路规则，不能悄悄退化为扣减总库存。
- Controller 是否保持薄层，业务判断是否复用已有 Service，而不是在入口重复实现。
- 数据库、Redis 和 MQ 失败时，返回状态是否忠实反映“成功、失败、处理中或结果未知”。

### 5.2 Python Agent 与 Tool

- `user_id`、运营身份等安全参数是否来自已验证上下文，而不是由模型自由填写。
- Tool 是否只暴露完成任务所需的最小参数，输入和输出是否经过 Schema 校验。
- Tool、Service 和业务客户端之间是否只有一个业务能力实现；Tool 不应复制业务规则。
- 模型负责理解和表达，确定性规则、计算、权限与写操作应由代码或后端服务完成。
- Tool 失败、超时和返回格式异常时，模型是否会得到明确、不可误解的结果。
- `X-Request-ID` 等关联信息是否在跨服务调用中透传，敏感数据是否避免进入用户可见回答。

### 5.3 Skill

- Skill 的真实指令正文是否来自对应 `SKILL.md`，不能只注册名称和 Front Matter 就声称模型使用了 Skill。
- Skill 何时加载、加载哪一份以及如何进入模型上下文必须可以从代码和轨迹中验证。
- `SKILL.md` 描述的是模型完成任务的方法；确定性业务编排类应命名为 Service 或其他真实职责，不能仅因目录名而包装成 Skill。
- Skill 指令不得覆盖身份、权限、确认、幂等和事实来源等系统级约束。

### 5.4 RAG 与知识库

- 检索前是否按用户、运营等受众边界限制候选知识，不能只依赖相似度事后过滤。
- 索引是否与当前文档版本一致；过期索引不得静默提供答案。
- 新旧规则冲突时是否按生效时间和优先级处理，而不是让模型自行选择。
- 回答中的关键规则是否能由实际召回内容支撑，引用是否确实对应结论。
- 实时业务数据不得写进静态知识后作为当前事实回答。
- 无关问题、错别字和口语输入不能通过降低阈值而误召回越权或无关知识。

### 5.5 长期记忆

- 记忆是否按可信用户身份隔离，不能因会话 ID 或模型参数串到其他用户。
- 保存内容是否忠实于用户明确表达的原始语义，不能为了内部 Schema 强迫用户补充当前不需要的信息。
- 记忆召回是否与当前问题相关，是否可能覆盖实时积分、库存、订单等业务事实。
- 冲突、更新、过期和删除后的行为是否明确；旧偏好不能无限期压过用户的新表达。
- 内部记忆字段、存储结构和调试信息不得直接暴露给用户。

### 5.6 运营 Agent

- 草案、提交、审核、发布和效果落账是否仍是不同状态，模型不能用自然语言越过工作流。
- 用户提问应先得到直接答案，能力边界只在确实影响任务时简短补充。
- 缺少业务参数时，Agent 应先使用已有发现或查询能力；不能把内部 ID 查询责任直接推给用户。
- 指标口径、分子、分母、时间窗、客群和数据来源必须明确，计算应由确定性代码完成。
- 模拟触达和模拟收益必须可见标注，不能声称发生了真实发送、阅读或兑换。

### 5.7 Web 前端

- 用户端与运营端身份、入口和权限表现是否隔离。
- 加载、处理中、成功、失败和结果未知是否有不同反馈，不能把 HTTP 成功等同于业务成功。
- 页面刷新、重复点击和网络重试是否会触发重复写操作。
- 对话区域滚动、窗口尺寸和错误提示是否让用户能看到完整结果并继续操作。
- 前端展示字段是否与后端状态语义一致，不能自行推断或改写核心状态。

### 5.8 跨服务契约

- Java DTO、Python Pydantic 模型和 TypeScript 类型是否表达同一字段含义与可空性。
- HTTP 状态码、业务码和用户提示是否分层，不能丢失“请求失败”与“业务失败”的区别。
- 超时、连接失败和响应解析失败是否有不同处理；写请求结果未知必须单独表达。
- 新增字段是否兼容旧调用方，删除或改名是否同步更新全部入口。
- 请求关联标识是否贯穿前端、Agent 和 Java 日志，便于还原一次真实调用。

## 6. 评审方法

一次有效评审至少完成以下步骤：

1. 阅读需求、验收条件和相关不可破坏项。
2. 查看本次差异，同时读取被修改代码的直接调用方和被调用方。
3. 沿真实链路检查一次正常流程、一次典型失败流程和一次重复或并发流程。
4. 区分源码事实、运行证据和评审推断；推断必须明确标注。
5. 先输出发现，再决定修复；不能用“测试通过”代替代码审查。
6. 修复后运行与改动直接相关的定向测试；跨模块里程碑再做必要的集成或端到端验证。
7. 记录未验证范围和剩余风险，不用无关的全量回归制造完成感。

当用户明确要求“代码评审”时，默认只报告问题，不直接修改代码；当 Code Review 是实现流程中的自检步骤时，可以记录发现并继续修复。

## 7. Findings 输出格式

发现必须按严重程度从高到低排列，每条使用以下格式：

```markdown
### [High] 写请求结果未知时被自动重试

- 位置：`path/to/file.py:123`
- 证据：超时异常与连接前失败进入同一个重试分支。
- 触发方式：后端已经受理写请求，但客户端在收到响应前超时。
- 后果：同一业务动作可能被执行两次；若下游幂等失效会重复扣减。
- 建议：将结果未知单独建模，不自动重试，并引导调用方先查询最终状态。
- 验证：增加“后端已执行但响应超时”的定向用例。
- 剩余风险：仍需确认 Java 端幂等键的持久化范围。
```

位置必须尽量精确到文件和行号。只有观点、没有证据和触发条件的内容不算有效 finding。

如果没有发现问题，应明确写：

```markdown
未发现会阻塞交付的代码问题。

剩余风险：尚未执行真实 Redis/MQ 环境下的端到端验证，本次结论仅覆盖代码审查和定向测试。
```

## 8. 评审结论

评审最终使用以下三种结论：

- `CHANGES_REQUIRED`：存在未解决的 `Critical` 或 `High`；禁止交付。
- `PASS_WITH_RISKS`：没有阻塞项，但存在已明确接受的 `Medium`、未验证范围或环境限制。
- `PASS`：没有阻塞项，验收条件已满足，必要验证已完成，剩余风险可忽略。

评审结论只代表当前差异和已声明验证范围，不能外推为整个项目没有问题。

## 9. 规则维护

- `CODE_REVIEW.MD` 保存全仓库长期有效的共性规则；`AGENTS.md` 只引用本文件，不复制完整清单。
- 某次需求独有的评审项写入 `openspec/changes/<change>/`，完成后仅将仍长期有效的规则提炼到本文。
- 只有重复出现的问题、已逃逸缺陷、明确架构变化或高风险事故经验，才应新增全局规则。
- 能由类型、测试、Lint、Harness 或 CI 稳定检查的规则，应逐步自动化，不要永久依赖人工记忆。
- 避免维护容易过期的完整类名和接口名清单，优先记录职责、边界和可观察行为。
- 降低身份、权限、事务、确认、幂等或写操作约束必须经过人工明确批准，不能由 Agent 自行修改规则。
- 规则变化通过 Git 历史追踪；不为每次微调额外维护版本号和重复变更日志。

## 10. 当前门禁状态

当前以人工和 Coding Agent 执行为主，尚未建立统一的 PR 模板、独立审批人或 CI 强制门禁。因此：

- 本文是评审基线，不代表规则已经被自动执行；
- 功能开发者应在交付前按本文自检；
- 高风险或跨模块改动仍需人工查看差异、findings 和验证证据；
- 后续只在真实协作需要出现时，再引入 PR 模板、CODEOWNERS 或 CI 门禁，不提前搭建形式化流程。
