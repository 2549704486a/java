# Coding Agent Repository Guide

> 这是仓库导航，不是项目说明书。先用本页确定边界，再按任务逐层读取；不要一开始扫描全部源码、设计文档或课程材料。

## 1. 开始任务

1. 阅读 [PRODUCT.md](PRODUCT.md)，确认当前产品能力和明确未实现项。
2. 阅读 [项目上下文入口](docs/agent-context/README.md) 与 [上下文清单](docs/agent-context/00_上下文清单.md)。
3. 阅读 [最高优先级路线图](docs/agent-design/20_Agent课程能力落地路线图.md)，确认当前阶段。
4. 根据任务类型，从上下文清单选择专项文档和源码；不要顺序读取全部 `agent-design/`。
5. 若需求跨服务、改变状态机或权限边界、引入数据结构，先使用 OpenSpec 收敛问题和方案：探索后创建 `openspec/changes/<change>/`，人工审阅规划工件后再进入实现。

## 2. 事实来源

发生冲突时按以下顺序判断：

1. 当前源码、接口契约和数据库迁移。
2. 当前生效的配置及可复现的运行证据。
3. `openspec/specs/` 中已归档并生效的行为契约。
4. `docs/agent-context/` 中经源码核对的项目事实。
5. `docs/agent-design/` 中的当前方案和路线图。
6. `docs/agent-journal/`、课程资料及历史说明。

`openspec/changes/` 是计划或正在实施的变更，不代表当前系统已经具备相应能力。文档中的提交号、完成状态和接口清单都可能过期；引用前必须与当前源码核对，不能把计划、Fixture 或模型推断写成已经实现的事实。

## 3. 目录职责

| 路径 | 职责 | 何时读取 |
| --- | --- | --- |
| `incentive/` | Java 业务服务、旧事务消息链路、活动工作流和数据访问 | 修改业务规则、接口、状态或持久化时 |
| `agent-service/` | 用户 Agent、运营 Agent、Tool、Skill、记忆、RAG 与 HTTP 服务 | 修改 Agent 行为或业务能力接入时 |
| `web-ui/` | 用户奖品中心、对话页面和运营工作台 | 修改可见交互时 |
| `docs/agent-context/` | 当前项目事实、旧链路和代码索引 | 每项任务按需读取 |
| `docs/agent-design/` | 已实施方案、取舍、评测与最高优先级路线图 | 需要理解某项能力为何存在时 |
| `openspec/specs/` | 已归档并生效的行为契约 | 核对系统承诺的外部行为时 |
| `openspec/changes/` | 复杂变更的提案、规格、设计和任务 | 跨模块、高风险或存在非显然取舍时 |
| `.agents/skills/openspec-*/` | OpenSpec 的 Explore、Propose、Apply、Archive 等 Agent 工作流 | 执行对应 OpenSpec 阶段时按需加载 |
| `docs/agent-journal/` | 已完成任务的简要时间线 | 完成并验证后更新 |
| `docs/agent-interview/` | 有代码或证据支撑的面试表达 | 仅在整理面试材料时读取 |
| `docs/agent-learning/` | 历史学习资料 | 仅在用户明确要求学习时读取 |

运行和日志位置见 [本地环境一键启动](docs/agent-design/05_本地环境一键启动.md)。各模块的具体命令以模块 README 为准。

## 4. 不可破坏的产品边界

- 兑换只保留旧事务消息链路；不得恢复或假设新链路存在。
- 不读取或引用 `ops/`、`.tmp_ops/`、压测文档、压测结果和历史性能数字作为 Agent 开发上下文。
- 实时积分、库存、订单、活动状态等动态事实必须来自业务接口，不能由 RAG、长期记忆或模型补全。
- 模型不得直接执行任意 SQL、Redis 命令、库存修改、积分修改或跳过活动审核发布。
- 兑换等高风险动作必须保留可信身份、明确确认、幂等、确定性服务和审计边界。
- 运营草案由 Agent 辅助生成；提交、审核、发布和效果落账由权限校验后的确定性工作流执行。
- `FIXTURE`、`SIMULATED` 和本地演示数据只能证明流程可运行，不能包装成真实运营收益。
- MCP 当前只有本地只读样本和显式 Agent 实验模式，不属于默认生产基线；多 Agent 尚未落地。扩展第二个 MCP Tool 或引入多 Agent 前，必须满足路线图中的业务前置条件和收益证据。

## 5. 开发协议

### 先定义问题

- 先判断任务复杂度。目标和边界明确的小型修改可以直接实现并做最小验证；跨服务、改变状态机或权限边界、引入数据结构、存在非显然取舍的需求按复杂变更处理。
- 先回答用户真正要解决的问题，不要急于罗列能力边界或技术清单。
- 将事实、假设、替代解释和待确认问题分开记录。
- 存在非显然取舍时，先比较最小方案、替代方案及不做的后果，再实施。
- 复杂变更先使用 `$openspec-explore` 调查和发散，再用 `$openspec-propose` 生成规划工件；提案经审阅后才使用 `$openspec-apply-change` 实施，验收完成后再归档。
- 小型修复、注释和纯文档整理无需机械创建 OpenSpec change。

### 复杂需求的 AI 辅助开发流程

复杂需求不得从模糊描述直接进入编码，统一按照以下顺序推进：

```text
准备项目上下文
  -> Brainstorming 与需求澄清
  -> 明确目标、非目标、边界和验收条件
  -> 形成并审阅 Spec
  -> 拆成可独立实现和验证的小任务
  -> 实现、Code Review、定向测试、修正
  -> 集成或端到端验收
  -> 人工确认交付
```

- 上下文至少覆盖当前源码、产品事实、相关接口或数据契约、历史决策、不可破坏边界和已知失败证据；按需加载，不以全仓库扫描代替问题理解。
- Brainstorming 先探索需求遗漏、冲突、权限、异常流程、兼容性、数据一致性、回滚和不实施的后果；没有完成问题收敛前不得用单一方案直接覆盖其他可能性。
- Spec 中每个行为必须具有可执行的验收条件，并明确涉及模块、事实来源、不允许改变的边界、失败场景和非目标；计划、假设和已实现事实必须分开。
- 任务按依赖顺序拆分，每项只交付一个可观察结果，并能够独立评审和验证；完成对应实现和证据后再进入下一项，避免一次生成大批不可审查的改动。
- Code Review 必须遵循根目录 [CODE_REVIEW.MD](CODE_REVIEW.MD)；优先报告行为错误、边界破坏和回归风险，并按统一严重级别、findings 模板和交付结论输出。
- 测试范围按改动影响确定：小步先执行关联单元测试和定向检查，跨模块里程碑再执行必要的集成或端到端验收；不以无关的全量回归制造完成感，也不能用局部单测替代真实业务闭环。
- 人工把守两端：实施前确认需求、取舍和验收标准，交付前检查代码、评审结论、测试证据、实际行为和剩余风险。不得把 AI 声称“已完成”作为验收依据。

### 再修改代码

- 只读取和修改完成当前任务所需的文件。
- 优先复用现有 Tool、Skill 和确定性业务服务，不建立平行业务实现。
- 不为未来可能出现的 MCP、多 Agent 或通用平台提前增加抽象。
- 文档描述当前事实或明确标注的计划，不复制容易漂移的源码清单。

### 完成任务

- 只执行与改动直接相关的检查；除非处于发布节点或用户明确要求，不机械运行全量回归。
- 功能完成并有证据后，再更新路线图覆盖情况和任务日记。
- 功能任务使用中文提交信息；纯文档微调可以随下一项相关功能提交。
- 汇报必须区分：已实现、验证证据、剩余风险、未包含范围。

## 6. 禁止范围蔓延

- 不顺手改造无关目录、统一全仓库风格或重写已有文档。
- 不把 Prompt 当作所有问题的补丁堆；可确定判断应进入代码、契约或 Harness。
- 不为了让当前案例通过而降低身份、确认、权限、幂等和证据要求。
- 不把课程中的技术示例反推为项目已经实现的能力。
