# Coding Agent Repository Guide

> 这是仓库导航，不是项目说明书。先用本页确定边界，再按任务逐层读取；不要一开始扫描全部源码、设计文档或课程材料。

## 1. 开始任务

1. 阅读 [PRODUCT.md](PRODUCT.md)，确认当前产品能力和明确未实现项。
2. 阅读 [项目上下文入口](docs/agent-context/README.md) 与 [上下文清单](docs/agent-context/00_上下文清单.md)。
3. 阅读 [最高优先级路线图](docs/agent-design/20_Agent课程能力落地路线图.md)，确认当前阶段。
4. 根据任务类型，从上下文清单选择专项文档和源码；不要顺序读取全部 `agent-design/`。
5. 若需求跨服务、改变状态机或权限边界、引入数据结构，先使用 [Feature Spec 模板](docs/specs/TEMPLATE.md) 收敛问题和方案。

## 2. 事实来源

发生冲突时按以下顺序判断：

1. 当前源码、接口契约和数据库迁移。
2. 当前生效的配置及可复现的运行证据。
3. `docs/agent-context/` 中经源码核对的项目事实。
4. `docs/agent-design/` 中的当前方案和路线图。
5. `docs/agent-journal/`、课程资料及历史说明。

文档中的提交号、完成状态和接口清单都可能过期。引用前必须与当前源码核对；不能把计划、Fixture 或模型推断写成已经实现的事实。

## 3. 目录职责

| 路径 | 职责 | 何时读取 |
| --- | --- | --- |
| `incentive/` | Java 业务服务、旧事务消息链路、活动工作流和数据访问 | 修改业务规则、接口、状态或持久化时 |
| `agent-service/` | 用户 Agent、运营 Agent、Tool、Skill、记忆、RAG 与 HTTP 服务 | 修改 Agent 行为或业务能力接入时 |
| `web-ui/` | 用户奖品中心、对话页面和运营工作台 | 修改可见交互时 |
| `docs/agent-context/` | 当前项目事实、旧链路和代码索引 | 每项任务按需读取 |
| `docs/agent-design/` | 已实施方案、取舍、评测与最高优先级路线图 | 需要理解某项能力为何存在时 |
| `docs/specs/` | 尚待实现或正在实施的复杂功能决策 | 跨模块、高风险或需求不清时 |
| `docs/agent-journal/` | 已完成任务的简要时间线 | 完成并验证后更新 |
| `docs/agent-interview/` | 有代码或证据支撑的面试表达 | 仅在整理面试材料时读取 |
| `docs/agent-learning/` | 历史学习资料 | 仅在用户明确要求学习时读取 |

运行和日志位置见 [本地环境一键启动](docs/agent-design/05_本地环境一键启动.md)。各模块的具体命令以模块 README 为准。

## 4. 不可破坏的产品边界

- 兑换只保留旧事务消息链路；不得恢复或假设新链路存在。
- 不读取或引用 `ops/`、`.tmp_ops/`、压测文档、压测结果和历史性能数字作为 Agent 开发上下文。
- 实时积分、库存、订单、活动状态等动态事实必须来自业务接口，不能由 RAG、长期记忆或模型补全。
- 模型不得直接执行任意 SQL、Redis 命令、库存修改、积分修改或跳过活动审核发布。
- 兑换等高风险动作必须保留可信身份、明确确认、幂等、确定性服务和审计边界。
- 运营草案由 Agent 辅助生成；提交、审核、发布和效果落账由权限校验后的确定性工作流执行。
- `FIXTURE`、`SIMULATED` 和本地演示数据只能证明流程可运行，不能包装成真实运营收益。
- MCP 与多 Agent 当前不属于已落地基线；只有路线图中的业务前置条件和收益证据成立后才能重新引入。

## 5. 开发协议

### 先定义问题

- 先回答用户真正要解决的问题，不要急于罗列能力边界或技术清单。
- 将事实、假设、替代解释和待确认问题分开记录。
- 存在非显然取舍时，先比较最小方案、替代方案及不做的后果，再实施。
- 小型修复、注释和纯文档整理无需机械创建 Spec。

### 再修改代码

- 只读取和修改完成当前任务所需的文件。
- 优先复用现有 Tool、Skill 和确定性业务服务，不建立平行业务实现。
- 不为未来可能出现的 MCP、多 Agent 或通用平台提前增加抽象。
- 文档描述当前事实或明确标注的计划，不复制容易漂移的源码清单。

### 完成任务

- 只执行与改动直接相关的检查；除非处于发布节点或用户明确要求，不机械运行全量回归。
- 功能完成并有证据后，再更新路线图覆盖情况和任务日记。
- 功能任务使用中文提交信息；纯文档微调可以随下一项相关功能提交。
- 汇报必须区分：已实现、验证证据、剩余风险、未包含范围。

## 6. 禁止范围蔓延

- 不顺手改造无关目录、统一全仓库风格或重写已有文档。
- 不把 Prompt 当作所有问题的补丁堆；可确定判断应进入代码、契约或 Harness。
- 不为了让当前案例通过而降低身份、确认、权限、幂等和证据要求。
- 不把课程中的技术示例反推为项目已经实现的能力。
