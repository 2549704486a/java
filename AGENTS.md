# 仓库开发指南

## 最小上下文

- 本文件是唯一默认必读的项目文档；当前会话已提供或读过且未变化时，复用已有内容。
- 以用户本次需求和已确认的当前任务为导向。简单解释直接回答，不自动转成开发或文档任务。
- 默认不读取、不搜索正文、不维护能力落地路线图、`01_Agent课程索引.md`、课程讲义/逐字稿、`agent-learning/` 等全部课程内容。只有用户在当前提问中明确要求加载时才读取指定部分；提到某项技术、要求学习或历史上要求课程覆盖，不等于授权加载课程。
- `PRODUCT.md`、`docs/agent-context/`、`docs/agent-design/`、任务日记和面试资料均非必读。只在解决当前具体疑问时读取相关章节，不沿文档链接递归加载。
- 用户最新要求优先；事实以当前源码、接口和运行证据为准。当前任务规格优先于旧计划，旧文档不能自动恢复为开发要求。

## 定位与读取

- Java 在 `incentive/`，Python Agent 在 `agent-service/`，网页在 `web-ui/`。
- 先在对应模块用 `rg -n` 或文件名定位，再读相关函数及直接调用方。无法定位时才扩大搜索范围。
- 不因 IDE 展示打开文件就读取正文；不默认扫描全仓库、全部设计文档、日志或运行数据。
- 默认输出命中行、局部代码、统计或错误。大文件、JSON、diff 和日志分段读取，结果截断时缩小查询，不提高上限重发全文。
- 同一任务复用已读上下文，续接时先看当前差异和任务进度；不重复启动阅读流程。交接只保留目标、决定、涉及路径、验证结果和下一步。
- 工具和 Skill 按实际需要使用；不为节省时间开启重复研究的子 Agent。需要委派时只传相关材料，返回结论和证据位置。

## 业务边界

- 兑换只走现有旧事务消息链路；不得恢复已删除的新链路。
- 实时积分、库存、订单和活动状态来自业务接口，不能由模型、RAG 或记忆补全。
- 模型不得执行任意 SQL、Redis 命令或绕过审核修改业务数据。
- 兑换保留可信身份、明确确认、幂等和审计；运营提交、审核、发布由确定性工作流执行。
- `FIXTURE`、`SIMULATED`、计划和实验结果不得描述为真实生产收益或已上线能力。
- 不读取或引用 `ops/`、`.tmp_ops/`、压测资料和历史性能数字作为开发上下文。
- MCP、多 Agent 等能力现状按本次涉及的源码核对；不为未来能力增加空层或重复实现。

## 开发与验证

- 小修复和文档调整直接处理。复杂需求先讨论问题与方案取舍，明确目标、边界和验收，再用 OpenSpec 拆解实施；已有授权和规划继续复用，不反复确认。
- OpenSpec 只读取当前 change 的任务及本步相关规格/设计，不通读所有变更或复制背景。
- 改代码时遵循 `CODE_REVIEW.MD` 的通用项与受影响模块项，已读内容不重复加载；纯讨论和文档微调无需加载评审指南。
- 只运行受影响行为的必要检查；通过后没有新变更或明确疑点，不重复回归。用户约定的验证范围持续生效。
- 保留用户未提交改动，不暂存无关文件。功能提交使用中文，并关联对应 `agent-design` 文档；沿用用户确认的提交粒度，纯文档微调不单独提交。

## 交付与维护

- 更新真正受影响的当前规格或一份相关文档即可；禁止例行同步路线图、课程覆盖表、上下文清单、日记等多份材料。
- 任务日记仅在用户要求或阶段性交接确有必要时简记，不读取整份历史。设计文档仅在行为或决策发生变化时维护。
- 汇报简短说明结果、必要验证和未完成项；不重复计划、工具输出或完整清单。不在已完成需求外追加工作。
