# Agent 项目任务日记

> 用简短时间线记录“什么时候完成了什么”。需要了解设计、代码或证据时，再沿产物链接进入对应专项文档。

## 2026-08-20

### 12:32｜建立项目上下文包

- 任务概述：读取当前积分秒杀项目代码，梳理项目全景、旧事务消息链路、数据一致性、代码配置索引、Agent 接入边界和课程渐进加载索引。
- 主要产物：`docs/agent-context/`。
- 说明：历史补录，时间依据上下文文档最后修改时间，不代表精确完成时刻。

### 13:17｜完成第一版最小 Agent

- 任务概述：实现只读积分规划 Agent、Java 查询接口、Function Calling Tools 和确定性积分规划 Skill；完成环境配置、离线测试和运行说明。
- 主要产物：`agent-service/`、Java `/agent/query/**` 接口、最小 Agent 设计与面试阶段文档。
- 说明：历史补录，时间依据阶段文档最后修改时间，不代表精确完成时刻。

### 13:28｜完成本地环境一键启动

- 任务概述：将 MySQL、Redis、RocketMQ、Canal、Dashboard 和 Spring Boot 应用整合为统一启动与状态检查流程，补充日志位置和使用说明。
- 主要产物：`start-local.ps1`、`start-local.bat`、`docs/agent-design/05_本地环境一键启动.md`。
- 说明：历史补录，时间依据启动文档最后修改时间，不代表精确完成时刻。

### 14:53｜建立技术学习清单

- 任务概述：从当前 Agent 代码、依赖和测试反向提取技术点，记录学习状态、代码证据和总结去向，防止开发完成后遗漏 Pydantic、Function Calling、Tool/Skill 分层等关键技术。
- 主要产物：`docs/agent-learning/`，并接入文档导航和上下文清单。
- 说明：历史补录，时间依据学习文档最后修改时间，不代表精确完成时刻。

### 15:40｜完成第一轮 Agent 评测与调优

- 任务概述：建立 15 个固定场景和 2 个真实集成用例，实现 Tool、参数、后端路径、回答与安全检查；保存原始基线，完成 Prompt 单变量调优、人工复核、离线重评分和最终回归。
- 主要结果：固定场景由 `5/15` 提升到 `15/15`，真实集成最终 `2/2`，Tool 调用由 33 次降至 12 次。
- 主要产物：`agent-service/evals/`、`docs/agent-design/07_第一轮Agent评测报告.md`、`docs/agent-interview/02_Agent评测与调优.md`。

### 15:43｜建立通用任务日记

- 任务概述：新增独立任务日记及维护规则，用于持续记录 Agent 项目的需求、开发、测试、学习、部署和排查任务，不再依赖对话记忆还原进度。
- 主要产物：`docs/agent-journal/`，并接入文档导航和上下文清单。

### 15:58｜建立按任务提交规范

- 任务概述：约定每个可独立验收的任务在完成验证和日记更新后进行一次 Git 提交，提交信息使用中文简述；没有文件变化时不创建空提交。
- 主要产物：更新任务日记维护规则、上下文清单和版本库忽略规则，并为当前 Agent 阶段建立可追溯的代码基线。

### 16:23｜沉淀 Agent 评测升级方案

- 任务概述：复盘第一轮规则评分的能力边界和 E14 关键词假阴性，设计由代码硬约束、结果状态、执行轨迹、LLM Judge、人工校准、盲测与线上回流组成的分层评测体系。
- 主要产物：`docs/agent-design/08_Agent评测体系升级.md`，并接入文档导航、上下文路由和技术学习清单。

### 16:58｜完成 Skill 运行时标准化

- 任务概述：将积分规划 `SKILL.md` 接入 Agent 运行时，增加 Skill 发现、声明校验、动态 Tool 描述、调用时激活、版本哈希记录和损坏定义测试，同时保留确定性 Python 业务执行。
- 主要产物：`app/skills/registry.py`、`tests/test_skill_registry.py`、`docs/agent-design/09_Skill运行时标准化.md`。
- 验证结果：离线测试 `13/13`；模型首轮回归 `13/15`，根据失败轨迹修正后针对性复验 `2/2`、最终完整回归 `15/15`。

### 17:26｜完成 Agent HTTP 服务化

- 任务概述：将 CLI Agent 包装为 FastAPI 常驻服务，增加 `/health`、`/v1/chat`、Pydantic 请求校验、请求 ID、错误脱敏、共享业务连接和按用户隔离的有界 LRU Agent 缓存，并接入本地一键启动脚本。
- 主要产物：`app/web.py`、`app/server.py`、`app/runtime.py`、`tests/test_web.py`、`tests/test_runtime.py`、`docs/agent-design/10_Agent服务化.md`。
- 验证结果：离线测试 `18/18`；真实 Uvicorn 健康检查为 `UP`、非法请求返回 `422`；完整 HTTP → 模型 → Tool → Java 链路返回 `200`。

### 18:37｜完成短期记忆与会话隔离

- 任务概述：为常驻 Agent 增加基于 `InMemorySaver` 的进程内短期记忆，以 `user_id + session_id` 组合隔离上下文；增加同会话串行锁、会话 LRU 淘汰和检查点清理，避免消息乱序及会话数量无限增长。
- 主要产物：`app/agent.py`、`app/runtime.py`、`app/web.py`、`evals/multiturn_runner.py`、`docs/agent-design/11_短期记忆与会话隔离.md`。
- 验证结果：离线测试 `20/20`；真实 HTTP 验证同一会话可理解“它”，新会话要求重新提供奖品 ID；多轮 Fixture 评测 `1/1` 个场景、`2/2` 个轮次通过。

### 19:08｜完成第二个业务 Skill

- 任务概述：新增只读奖品推荐 Skill，编排实时积分与奖品资格查询，通过 Pydantic 校验嵌套数据，并使用确定性代码过滤、排序当前可兑换奖品；Prompt 增加基础列表、积分规划和奖品推荐的路由边界。
- 主要产物：`app/skills/award_recommendation.py`、`skills/award-recommendation/SKILL.md`、`tests/test_award_recommendation.py`、`docs/agent-design/12_奖品推荐Skill.md`。
- 验证结果：单元测试 `25/25`；真实 Skill 与 HTTP 调用成功；完整 Fixture 回归 `18/18`。E18 的可选默认参数评分假阴性通过原始结果离线重评分修正，没有重新调用模型。

### 19:42｜重建本地业务演示数据

- 任务概述：备份并清理历史压测随机数据，为 20 个用户、6 个任务和 6 个奖品生成关系一致、可解释的业务数据；清空 Redis 并重启应用完成缓存重建。
- 主要产物：`sql/seed_demo_budou.sql`、`docs/agent-design/13_本地演示数据基线.md`。
- 排查记录：首次导入后旧库存被 `DataSyncService` 从 Redis 反向覆盖，修正为“停应用、清 Redis、导 MySQL、再启动”的顺序后，MySQL、Redis 与接口库存一致。
- 验证结果：五类数据库一致性检查异常数均为 `0`；两个真实 Skill 返回符合预期；Agent 单元测试 `25/25`。

### 19:56｜完成 Tool 调用链可观测性

- 任务概述：新增请求级 Tool/Skill 结构化执行轨迹，以 `request_id` 关联实际调用参数、完成状态、结果码和耗时；评测器改为基于实际执行轨迹评分并聚合 Tool 指标。
- 主要产物：`app/trace.py`、`tests/test_trace.py`、`docs/agent-design/14_Tool调用链可观测性.md`。
- 验证结果：离线测试 `29/29`；新增多 Skill 用例 `4/4`；固定场景全量回归 `22/22`；真实 HTTP 请求与日志轨迹能够通过同一 `request_id` 对应。

### 20:49｜补充调用链轨迹关键注释

- 任务概述：沿 `web -> runtime -> agent -> Tool/Skill -> Java API` 调用链补充步骤级注释，说明请求 ID、轨迹上下文、统一执行包装、异常记录和评测取数之间的关系。
- 代码行为：仅增加解释设计意图的注释，不调整业务逻辑、轨迹字段和评测规则。
- 验证结果：Agent 离线测试 `29/29` 通过。

### 21:07｜完成受控兑换 Skill 方案设计

- 任务概述：基于当前旧事务消息链路设计“准备兑换、一次性确认、原子执行”的高风险写操作状态机，明确身份、时效、幂等、未知结果和审计边界。
- 主要产物：`docs/agent-design/15_受控兑换Skill需求与状态机.md`、`16_受控兑换Tool契约.md`、`17_受控兑换评测用例.md`。
- 验证结果：完成 `X01` 至 `X33` 共 33 个评测场景的一致性检查；根据源码确认旧 `502` 无法区分刚受理和此前处理中，统一采用 `EXCHANGE_PROCESSING`。当前运行代码仍保持只读。
