# Agent 项目任务日记

> 用简短时间线记录“什么时候完成了什么”。需要了解设计、代码或证据时，再沿产物链接进入对应专项文档。

## 2026-08-20

### 12:32｜建立项目上下文包

- 任务概述：读取当前积分秒杀项目代码，梳理项目全景、旧事务消息链路、数据一致性、代码配置索引、Agent 接入边界和课程渐进加载索引。
- 主要产物：`docs/agent-context/`。
- 说明：历史补录，时间依据上下文文档最后修改时间，不代表精确完成时刻。

### 13:17｜完成第一版最小 Agent

- 任务概述：实现只读积分规划 Agent、Java 查询接口、Function Calling Tools 和确定性积分规划 Skill；完成环境配置、离线测试和运行说明。
- 主要产物：`agent-service/`、Java `/agent/query/**` 接口、最小 Agent 设计与面试阶段文档。
- 说明：历史补录，时间依据阶段文档最后修改时间，不代表精确完成时刻。

### 13:28｜完成本地环境一键启动

- 任务概述：将 MySQL、Redis、RocketMQ、Canal、Dashboard 和 Spring Boot 应用整合为统一启动与状态检查流程，补充日志位置和使用说明。
- 主要产物：`start-local.ps1`、`start-local.bat`、`docs/agent-design/05_本地环境一键启动.md`。
- 说明：历史补录，时间依据启动文档最后修改时间，不代表精确完成时刻。

### 14:53｜建立技术学习清单

- 任务概述：从当前 Agent 代码、依赖和测试反向提取技术点，记录学习状态、代码证据和总结去向，防止开发完成后遗漏 Pydantic、Function Calling、Tool/Skill 分层等关键技术。
- 主要产物：`docs/agent-learning/`，并接入文档导航和上下文清单。
- 说明：历史补录，时间依据学习文档最后修改时间，不代表精确完成时刻。

### 15:40｜完成第一轮 Agent 评测与调优

- 任务概述：建立 15 个固定场景和 2 个真实集成用例，实现 Tool、参数、后端路径、回答与安全检查；保存原始基线，完成 Prompt 单变量调优、人工复核、离线重评分和最终回归。
- 主要结果：固定场景由 `5/15` 提升到 `15/15`，真实集成最终 `2/2`，Tool 调用由 33 次降至 12 次。
- 主要产物：`agent-service/evals/`、`docs/agent-design/07_第一轮Agent评测报告.md`、`docs/agent-interview/02_Agent评测与调优.md`。

### 15:43｜建立通用任务日记

- 任务概述：新增独立任务日记及维护规则，用于持续记录 Agent 项目的需求、开发、测试、学习、部署和排查任务，不再依赖对话记忆还原进度。
- 主要产物：`docs/agent-journal/`，并接入文档导航和上下文清单。

### 15:58｜建立按任务提交规范

- 任务概述：约定每个可独立验收的任务在完成验证和日记更新后进行一次 Git 提交，提交信息使用中文简述；没有文件变化时不创建空提交。
- 主要产物：更新任务日记维护规则、上下文清单和版本库忽略规则，并为当前 Agent 阶段建立可追溯的代码基线。

### 16:23｜沉淀 Agent 评测升级方案

- 任务概述：复盘第一轮规则评分的能力边界和 E14 关键词假阴性，设计由代码硬约束、结果状态、执行轨迹、LLM Judge、人工校准、盲测与线上回流组成的分层评测体系。
- 主要产物：`docs/agent-design/08_Agent评测体系升级.md`，并接入文档导航、上下文路由和技术学习清单。

### 16:58｜完成 Skill 运行时标准化

- 任务概述：将积分规划 `SKILL.md` 接入 Agent 运行时，增加 Skill 发现、声明校验、动态 Tool 描述、调用时激活、版本哈希记录和损坏定义测试，同时保留确定性 Python 业务执行。
- 主要产物：`app/skills/registry.py`、`tests/test_skill_registry.py`、`docs/agent-design/09_Skill运行时标准化.md`。
- 验证结果：离线测试 `13/13`；模型首轮回归 `13/15`，根据失败轨迹修正后针对性复验 `2/2`、最终完整回归 `15/15`。

### 17:26｜完成 Agent HTTP 服务化

- 任务概述：将 CLI Agent 包装为 FastAPI 常驻服务，增加 `/health`、`/v1/chat`、Pydantic 请求校验、请求 ID、错误脱敏、共享业务连接和按用户隔离的有界 LRU Agent 缓存，并接入本地一键启动脚本。
- 主要产物：`app/web.py`、`app/server.py`、`app/runtime.py`、`tests/test_web.py`、`tests/test_runtime.py`、`docs/agent-design/10_Agent服务化.md`。
- 验证结果：离线测试 `18/18`；真实 Uvicorn 健康检查为 `UP`、非法请求返回 `422`；完整 HTTP → 模型 → Tool → Java 链路返回 `200`。

### 18:37｜完成短期记忆与会话隔离

- 任务概述：为常驻 Agent 增加基于 `InMemorySaver` 的进程内短期记忆，以 `user_id + session_id` 组合隔离上下文；增加同会话串行锁、会话 LRU 淘汰和检查点清理，避免消息乱序及会话数量无限增长。
- 主要产物：`app/agent.py`、`app/runtime.py`、`app/web.py`、`evals/multiturn_runner.py`、`docs/agent-design/11_短期记忆与会话隔离.md`。
- 验证结果：离线测试 `20/20`；真实 HTTP 验证同一会话可理解“它”，新会话要求重新提供奖品 ID；多轮 Fixture 评测 `1/1` 个场景、`2/2` 个轮次通过。

### 19:08｜完成第二个业务 Skill

- 任务概述：新增只读奖品推荐 Skill，编排实时积分与奖品资格查询，通过 Pydantic 校验嵌套数据，并使用确定性代码过滤、排序当前可兑换奖品；Prompt 增加基础列表、积分规划和奖品推荐的路由边界。
- 主要产物：`app/skills/award_recommendation.py`、`skills/award-recommendation/SKILL.md`、`tests/test_award_recommendation.py`、`docs/agent-design/12_奖品推荐Skill.md`。
- 验证结果：单元测试 `25/25`；真实 Skill 与 HTTP 调用成功；完整 Fixture 回归 `18/18`。E18 的可选默认参数评分假阴性通过原始结果离线重评分修正，没有重新调用模型。

### 19:42｜重建本地业务演示数据

- 任务概述：备份并清理历史压测随机数据，为 20 个用户、6 个任务和 6 个奖品生成关系一致、可解释的业务数据；清空 Redis 并重启应用完成缓存重建。
- 主要产物：`sql/seed_demo_budou.sql`、`docs/agent-design/13_本地演示数据基线.md`。
- 排查记录：首次导入后旧库存被 `DataSyncService` 从 Redis 反向覆盖，修正为“停应用、清 Redis、导 MySQL、再启动”的顺序后，MySQL、Redis 与接口库存一致。
- 验证结果：五类数据库一致性检查异常数均为 `0`；两个真实 Skill 返回符合预期；Agent 单元测试 `25/25`。

### 19:56｜完成 Tool 调用链可观测性

- 任务概述：新增请求级 Tool/Skill 结构化执行轨迹，以 `request_id` 关联实际调用参数、完成状态、结果码和耗时；评测器改为基于实际执行轨迹评分并聚合 Tool 指标。
- 主要产物：`app/trace.py`、`tests/test_trace.py`、`docs/agent-design/14_Tool调用链可观测性.md`。
- 验证结果：离线测试 `29/29`；新增多 Skill 用例 `4/4`；固定场景全量回归 `22/22`；真实 HTTP 请求与日志轨迹能够通过同一 `request_id` 对应。

### 20:49｜补充调用链轨迹关键注释

- 任务概述：沿 `web -> runtime -> agent -> Tool/Skill -> Java API` 调用链补充步骤级注释，说明请求 ID、轨迹上下文、统一执行包装、异常记录和评测取数之间的关系。
- 代码行为：仅增加解释设计意图的注释，不调整业务逻辑、轨迹字段和评测规则。
- 验证结果：Agent 离线测试 `29/29` 通过。

### 21:07｜完成受控兑换 Skill 方案设计

- 任务概述：基于当前旧事务消息链路设计“准备兑换、一次性确认、原子执行”的高风险写操作状态机，明确身份、时效、幂等、未知结果和审计边界。
- 主要产物：`docs/agent-design/15_受控兑换Skill需求与状态机.md`、`16_受控兑换Tool契约.md`、`17_受控兑换评测用例.md`。
- 验证结果：完成 `X01` 至 `X33` 共 33 个评测场景的一致性检查；根据源码确认旧 `502` 无法区分刚受理和此前处理中，统一采用 `EXCHANGE_PROCESSING`。当前运行代码仍保持只读。

## 2026-08-21

### 00:56｜完成奖品中心与 Agent 对话前端

- 任务概述：实现 React 奖品中心、用户切换与 Agent 会话页面，新增 FastAPI 首屏聚合接口，并将前端构建和同源静态托管接入本地一键启动。
- 主要产物：`web-ui/`、`GET /v1/dashboard/{user_id}`、`docs/agent-design/18_奖品中心与Agent前端.md`。
- 安全边界：页面仅支持实时资格查询和 Agent 咨询，不绕过受控兑换设计直接调用旧写链路。
- 验证结果：前端生产构建通过；Agent 离线测试 `30/30`；真实首页与用户 10 dashboard 均返回 `200`，dashboard 返回 `1680` 积分和 6 个奖品。

### 01:21｜完成受控兑换 Skill

- 任务概述：实现一次性兑换确认存储、用户与会话绑定、原子消费、取消与过期状态；新增 prepare/confirm/cancel Tool，并通过 Java POST 适配复用旧事务消息兑换链路。
- 安全边界：确认 POST 不自动重试；重复和并发确认最多调用一次 Java；处理中不包装为最终成功；提交结果未知时要求先核对订单。
- 主要产物：`app/confirmation_store.py`、`app/skills/controlled_exchange.py`、`skills/controlled-exchange/SKILL.md`、`AgentCommandController.java`、`docs/agent-design/19_受控兑换Skill实现.md`。
- 验证结果：Python 测试 `42/42`、Java 定向测试和前端构建通过；本地重部署后 3 个 Skill 正常发现。自然语言准备/取消及跳过确认攻击冒烟通过；安全 POST 返回 `EXCHANGE_REJECTED` 且未发送事务消息。

### 15:50｜完成受控兑换确定性闭环与缓存修复

- 任务概述：将确认/取消从模型 Tool 调用改为服务端白名单确定性路由，确认凭证只保存在服务端；前端增加待确认摘要卡片和明确操作按钮。
- 排查记录：真实兑换失败首先定位到默认仅预热 6 号奖品；恢复 Canal 位点后继续发现积分写入键为 `user_currency:<id>`、业务读取键为 `user:currency:<id>`，修正键名并补回归测试。
- 真实验证：用户 8 兑换 2 号奖品后，MySQL/Redis 积分同时由 `2300` 变为 `800`，分片库存总和同时由 `58` 变为 `57`，订单状态为 `1`，结果查询成功。
- 验证结果：Python `47/47`、Java `mvn test`、React 生产构建通过；模型完整回归 `23/25`、安全检查 `25/25`、受控兑换定向复验 `2/2`。

### 16:13｜整理 Agent 工程实践面试叙述

- 任务概述：将已实现的 Agent 边界、Tool/Skill、受控兑换、Pydantic 契约、评测、可观测性、短期记忆和服务化整理成可直接口述的面试文本。
- 内容边界：只使用当前代码、测试和评测能够证明的事实，明确单实例确认存储、Java 请求级幂等和长期记忆等尚未完成项；不包含基础设施闭环排查。
- 主要产物：`docs/agent-interview/04_Agent工程实践面试叙述.md`。

### 16:34｜建立 Agent 课程能力落地路线图

- 任务概述：将课程能力与当前项目进度映射为最高优先级路线图，明确先补受控写操作底座和评测，再按业务价值引入 RAG、记忆治理、MCP、部署能力和可选多 Agent。
- 维护规则：每项任务完成并验证后，必须同步更新课程覆盖表、当前里程碑、技术学习清单和任务日记；若开发顺序变化，还要记录调整原因和证据。
- 主要产物：`docs/agent-design/20_Agent课程能力落地路线图.md`，并已接入项目文档导航和渐进式上下文加载入口。

### 16:55｜简化 Agent 学习资料维护规则

- 任务概述：确认 `agent-learning/` 不参与开发优先级判断，将其从默认上下文和日常必填维护项中移除，保留已有内容作为按需复习资料。
- 路线图调整：课程覆盖表继续作为能力进度账本；本次未新增课程能力，因此覆盖状态不变，并在调整记录中注明维护规则变化。
- 使用方式：只有用户明确要求学习、复习或整理技术专题时，才加载和更新 `agent-learning/`。

### 17:10｜完成 Java 兑换请求持久化幂等

- 所属阶段：`P0.1 受控写操作工程底座`。
- 完成内容：新增 `agent_exchange_request` 请求表和 Java 幂等应用服务，在旧兑换链路前实现数据库占位、SHA-256 请求指纹、响应快照重放、同键异参冲突、并发唯一键竞争以及异常/过期执行转未知；消费侧业务幂等保持不变。
- 验证证据：Java 幂等服务 `6/6`、Controller `4/4`，Java 编译和打包通过，Agent Python 回归 `47/47`；本地数据库迁移成功，真实 HTTP 验证同键响应重放、同键异参冲突和单记录持久化均符合预期，冒烟数据已清理。
- 课程覆盖变化：Function Calling 增加写 Tool 持久化幂等证据；端到端实践增加真实 HTTP 与数据库闭环证据，状态均保持不变。
- 下一步：`P0.2` 将 Python `ConfirmationStore` 迁移到 Redis 共享存储，并验证多实例下的原子凭证消费。

### 18:00｜补充兑换幂等处理流程图

- 任务概述：在文档 21 的状态机部分增加 Mermaid 流程图，将幂等键校验、请求指纹、首次占位、唯一键竞争、旧链路调用、响应快照重放和未知状态处理串成完整流程。
- 设计重点：明确 `EXECUTING` 超时后转为 `UNKNOWN`，只允许查询或对账，不自动重放兑换请求，避免重复副作用。
- 课程覆盖变化：本次只补充已有实现的可视化说明，未新增工程能力，课程覆盖状态不变。
- 兼容性修正：沿用项目全景图已经验证的基础 Mermaid 写法，只使用普通节点和 `-->` 连线，将分支含义写入目标节点，兼容 Typora 使用的 Mermaid 10.1.0。

### 18:30｜完成 Redis 确认凭证共享存储

- 所属阶段：`P0.2 受控写操作工程底座`。
- 完成内容：抽取确认存储契约，新增 Redis Hash、会话待确认指针和容量索引；使用 Lua 原子实现创建、占用、完成、取消与过期处理，HTTP 服务默认使用 Redis，内存实现保留给单元测试。
- 安全边界：Redis 时间统一判断有效期；凭证记录与待确认指针分离；Agent 停机只关闭连接、不清空共享状态；Redis 不可用时启动失败，不静默降级。
- 验证证据：原有回归无退化；两个独立 Redis 客户端共享状态，20 线程跨实例竞争得到 `1` 个成功、`19` 个已使用；包含配置工厂和 Redis 专项用例的全量测试 `54/54` 通过。
- 课程覆盖变化：Function Calling 增加 Redis Lua 原子确认；端到端实践增加跨实例并发验证，课程状态均保持不变。
- 下一步：`P0.3` 明确可信用户身份来源，替代请求体直接传入 `user_id` 的演示边界。

### 18:45｜修复兑换后会话仍停留在待确认状态

- 所属阶段：`P0 受控写操作工程底座`。
- 问题证据：同一会话中确认请求已返回 `EXCHANGE_PROCESSING`，但随后追问的 Tool 轨迹为空；确定性确认路由绕过 LangGraph 后，没有把“确认指令和真实处理结果”写回会话历史，模型仍看到旧的待确认上下文。
- 完成内容：新增无模型调用的会话追加能力；确认或取消执行后，将用户指令和确定性业务结果写回同一 `thread_id`，记忆同步异常只记录日志，不改变已经发生的兑换结果。
- 验证证据：真实 `InMemorySaver + StateGraph` 验证消息顺序由“等待确认”更新为“确认兑换、兑换请求已进入处理流程”；全量 Python 测试及 Redis 集成测试 `55/55` 通过；Agent 服务已重启且 `/health` 为 `UP`。
- 课程覆盖变化：记忆系统增加确定性路由与 Agent 历史一致性证据；端到端实践增加兑换后追问语义回归，覆盖状态不变。

### 19:04｜完成 Agent 可信用户身份

- 所属阶段：`P0.3 受控写操作工程底座`。
- 完成内容：新增 HS256 JWT 签发与验证，从签名声明提取可信 `user_id`；聊天请求体和 dashboard 路径不再接收用户 ID；前端改为 Token 验证和当前用户展示。
- 安全边界：校验签名、签发方、受众、有效期和 `sub/uid` 一致性；注入额外 `user_id` 会被 Pydantic 拒绝；确认凭证继续绑定可信用户与会话。
- 验证证据：Token 本地签发命令验证通过；Python 离线回归 `60/60` 通过，7 个 Redis 集成用例按开关跳过；React TypeScript 检查和生产构建通过。
- 课程覆盖变化：Function Calling 增加可信身份绑定证据；P0.3 完成，下一项为固定端到端回归。
- 剩余问题：本地 HS256 签发不是正式登录系统；Java 内部接口在生产部署时仍需网络隔离和服务间认证。

### 20:28｜完成评测集隔离与重复试验框架

- 所属阶段：`P1 评测可信度建设`。
- 完成内容：保留原有公开用例作为调优/回归集，新增 8 个独立编号的冻结 Fixture 盲测场景；Runner 支持 `--dataset` 和 `--repeat`，输出逐用例通过率、单次/稳定/波动状态、耗时 P95 与标准差。
- 防泄漏边界：盲测控制台不打印问题和 Tool 路径，结果 JSON 不保存问题、回答、期望内容和完整轨迹；元数据记录用例集 SHA-256。
- 验证证据：评测模块静态编译通过；数据集隔离、重复聚合、单次结果语义和盲测脱敏等定向测试通过。本轮未调用真实模型，因此没有生成新的模型质量结论。
- 课程覆盖变化：课程 11 增加调优/盲测集隔离和重复试验稳定性统计证据，状态保持“部分落地”。
- 下一步：将关键安全场景升级为不可被平均通过率掩盖的发布硬门禁，并校准评分误判边界。

### 20:46｜完成模型耗时、Token 与成本评测指标

- 所属阶段：`P1 评测可信度建设`。
- 完成内容：使用 LangChain 模型回调记录每次 LLM 调用的客户端观测耗时；汇总输入、输出和总 Token、数据覆盖率，并支持通过显式输入/输出单价估算美元成本。
- 指标边界：Agent 端到端耗时、模型调用耗时和 Tool 耗时分别统计；未配置价格时成本标记为不可用，不按 0 元处理；模型耗时包含网络和供应商排队，不包装为纯推理耗时。
- 缺陷修复：修复汇总函数变量复用可能导致整体耗时被最后一个 Tool 耗时覆盖的问题，并增加回归测试。
- 验证证据：指标模块、Runner 和离线重评分脚本静态编译通过；模型成功/失败计时、Token 聚合、成本公式、模型耗时聚合和整体耗时隔离定向测试通过。本轮未调用真实模型。
- 课程覆盖变化：课程 11 增加模型/Tool/端到端耗时、Token 与可配置成本统计证据，状态保持“部分落地”。
- 下一步：按用户决定将安全硬门禁留作后续补充，进入 `P2.1` 业务规则知识源与 RAG 边界设计。

### 20:58｜建立受控 RAG 知识源与 Tool 边界

- 所属阶段：`P2.1 业务规则 RAG`。
- 完成内容：从当前源码提炼积分任务、兑换状态和 Agent 使用说明三类稳定知识；使用 YAML 元数据、文档版本、目录版本和项目内来源建立受控知识清单；明确积分、库存、价格、活动时间、资格和订单结果必须继续走实时 Tool。
- 工程边界：当前没有接入向量库和在线检索，知识文档不会自动进入模型上下文；`draft/retired` 文档即使通过完整性检查，也不会成为后续索引输入。
- 验证证据：知识目录校验 `3/3` 通过；目录穿越、重复 ID、来源缺失和必需章节缺失共 4 类坏输入均被拒绝，定向测试 `5/5` 通过。

### 21:06｜简化 RAG 知识版本治理

- 所属阶段：`P2.1 业务规则 RAG`。
- 调整原因：用户要求后续版本治理保持简单、可读，不再维护额外标识。
- 完成内容：知识清单只保留知识 ID 和路径，使用文档版本、目录版本、项目内来源与 Git 历史；相应异常测试替换为来源文件缺失测试。
- 验证边界：本次只调整新建的 RAG 知识源治理，不修改更早已经用于 Skill、评测、Redis 会话键或 Java 幂等的内部实现，避免破坏既有功能。
- 下一步：后续 RAG 切分、索引、检索和评测沿用这套简化版本规则。
- 课程覆盖变化：课程 5 从“未落地”调整为“部分落地”；课程中的离线知识准备和来源元数据已开始项目化，切分、向量索引、检索、引用与检索评测仍未完成。
- 下一步：`P2.2` 实现文档切分、Chunk 元数据和可重复构建的本地向量索引。

### 21:34｜完成 RAG 文档切分与本地向量索引

- 所属阶段：`P2.2 业务规则 RAG`。
- 完成内容：新增 Markdown 标题与递归字符两阶段切分，保留知识版本、来源、主题和章节等 Chunk 元数据；接入 Chroma 本地集合及 OpenAI 兼容 Embedding 配置，支持切分检查和完整重建同名集合。
- 工程边界：索引目录属于可重复生成的派生产物，不提交 Git；当前尚未接入 Agent 在线检索，也没有把外部 Embedding 结果包装成已交付能力。
- 验证证据：3 篇受控文档生成 12 个 Chunk；定向测试 `3/3`、持久化索引冒烟通过；全量 Python 测试共执行 `76` 个，`69` 个通过，`7` 个 Redis 集成测试按开关跳过。
- 课程覆盖变化：课程 5 增加切分、来源元数据、向量化接口和本地索引证据；课程 6 增加递归文档切分证据，状态均保持“部分落地”。
- 下一步：`P2.3` 实现只读在线检索、来源引用和低相关结果拒答。

### 22:10｜完成 RAG 只读检索代码与拒答契约

- 所属阶段：`P2.3 业务规则 RAG`。
- 完成内容：新增共享只读知识检索服务和可选 `search_business_knowledge` Tool；返回原文、相关度与用户可读引用，过滤低相关和重复 Chunk；RAG 关闭时同时移除 Tool 与 Prompt 声明。
- 工程增强：检索失败统一脱敏；轨迹只记录查询字符数；索引构建采用临时集合成功后替换正式集合，外部向量化失败不会破坏旧索引。
- 验证证据：可靠命中、引用、低相关无答案、底层异常、按能力注入、轨迹脱敏和失败安全建库等定向测试通过；全量 Python 测试共执行 `84` 个，`77` 个通过，`7` 个 Redis 集成测试按开关跳过。
- 真实联调：当前对话模型兼容服务的默认 Embedding 请求返回 `404`，模型列表无 Embedding 候选，因此没有生成真实外部索引，`RAG_ENABLED` 保持关闭。
- 课程覆盖变化：课程 5 增加在线检索 Tool、引用、拒答和失败安全构建证据，状态保持“部分落地”。
- 下一步：配置可用 Embedding 服务，完成真实建库、阈值校准和端到端引用验证。

### 22:59｜完成阿里云 Embedding 真实联调

- 所属阶段：`P2.3 业务规则 RAG`。
- 完成内容：使用阿里云 `text-embedding-v4` 将 3 篇受控文档的 12 个 Chunk 写入本地 Chroma；增加可配置建库批量大小，适配服务单批最多 10 条的限制；启用本机 RAG。
- 直接证据：三类业务问题最高相关度为 `0.6237`、`0.5891`、`0.6249`，均成功返回正文和来源引用；天气问题最高原始分数为 `-0.0164`，在 `0.35` 阈值下返回无相关知识；真实 `search_business_knowledge` Tool 调用成功。
- 工程修正：将欧氏距离换算结果约束到 `0~1`，消除无关查询的负分告警，不改变相关候选的排序与阈值判断。
- 课程覆盖变化：课程 5 增加真实向量服务、真实索引、阈值冒烟校准和在线 Tool 证据，状态保持“部分落地”；下一步建设正式检索评测集与引用正确性指标。

### 23:55｜完成 RAG 检索、路由与引用评测

- 所属阶段：`P2.4 业务规则 RAG`。
- 完成内容：建立 18 条检索用例和 7 条 Agent 用例；分别评测 Top1、Hit@3、无关问题拒答、引用结构、Tool 路由、知识命中和回答引用；Runner 保存逐用例原始证据并输出进度。
- 问题与修复：首次 Agent 评测为 `6/7`，唯一失败是正确知识已进入 Top3且回答内容正确，但模型遗漏引用；统一回答出口新增确定性引用兜底，只追加本轮知识 Tool 真实返回的来源。
- 验证证据：检索层 `18/18`，Top1、Hit@3、拒答和引用结构均为 `100%`；Agent 层修复后 `7/7`，Tool 选择、执行完成、知识命中、引用和必需内容均为 `100%`；全量测试执行 `93` 项，`86` 项通过，`7` 项 Redis 集成测试按开关跳过。
- 课程覆盖变化：课程 5 调整为“已落地”；当前没有证据要求引入 LCEL，因此课程 6 保持“部分落地”且不为覆盖课程强行增加抽象。
- 下一步：进入 `P3.1`，为会话上下文建立可观测的 token 预算和裁剪策略。

## 2026-08-22

### 11:07｜完成 RAG 面向用户表达与引用治理

- 所属阶段：`P2.5 业务规则 RAG`。
- 完成内容：把兑换和任务知识中的内部状态改为用户可理解的业务语言；稳定规则强制先走知识检索；统一回答出口去除重复引用，漏引时只补最相关的真实来源。
- 评测增强：Agent 用例新增内部术语禁用和单来源引用次数限制，分别形成 `user_language` 与 `citation_concise` 指标。
- 验证证据：检索评测 `18/18`、Agent 评测 `7/7`、定向单元测试 `14/14`；真实 HTTP 使用三个全新会话验证 `3/3`，日志均记录知识检索 Tool 轨迹。
- 排查记录：一次 PowerShell 冒烟返回英文澄清，最终通过 UTF-8 Python 客户端和服务端 Tool 轨迹确认是请求体中文编码失真，不是 RAG 依赖注入或路由失效。
- 课程覆盖变化：课程 2 增加通用 Prompt 表达治理证据；课程 5 增加用户语言、引用简洁性和真实 HTTP 证据，状态保持“已落地”。
- 下一步：进入 `P3.1`，为会话上下文增加可观测的 token 预算与裁剪策略。

### 11:40｜整理 RAG 工程实践面试叙述

- 完成内容：新增 RAG 专题面试文档，按照“面试官提问、可直接口述回答、可能追问、事实边界”的结构，整理知识与实时 Tool 边界、知识源治理、切分建库、在线检索、低相关拒答、来源引用、用户表达治理、分层评测和排查案例。
- 证据口径：使用当前真实实现和最新回归数据，记录 3 篇文档、12 个 Chunk、`0.35` 阈值、检索 `18/18`、Agent `7/7` 和真实 HTTP `3/3`；明确这些是当前冻结用例下的工程证据，不描述为生产准确率。
- 工程边界：混合检索、重排、复杂链式编排和索引新鲜度门禁均未包装成已实现能力；知识库规模和当前不足在文档中单独说明。
- 课程覆盖变化：无新增运行时能力，本次只把已经落地的课程 5 能力整理为可复习、可追问、可验证的面试叙述。

### 12:41｜完成 RAG 索引新鲜度与答案忠实度治理

- 所属阶段：`P2.6 业务规则 RAG`。
- 完成内容：RAG 启动时校验目录版本、知识 ID 和单篇文档版本，旧索引、混合版本和缺失元数据会拒绝启动；Agent 评测增加关键结论证据覆盖指标；新增错别字、口语和冲突陈述用例。
- 直接证据：旧阈值 `0.35` 下口语用例以 `0.3446` 被错误拒答，负样本最高仅 `0.0540`，因此阈值校准为 `0.30`；最终真实检索 `24/24`、Hit@3 和拒答率 `100%`、Top1 `94.44%`，真实 Agent `11/11`，适用的 7 条规则用例忠实度 `100%`。
- 评测修正：初始口语问题同时具有规则和实时状态含义，Agent 合理调用实时积分与任务 Tool；保留原始 `10/11` 结果，只把 Agent 用例改成明确询问规则的口语表达，未修改 Prompt 迎合用例。
- 验证证据：定向测试 `24/24`；全量 Python 测试执行 `101` 项，`94` 项通过，`7` 项 Redis 集成测试按环境开关跳过；评测摘要显式记录每项检查的通过数与适用用例数，忠实度分母为 `7`。
- 课程覆盖变化：课程 5 增加索引新鲜度门禁、关键结论忠实度和困难用例证据，状态保持“已落地”；没有证据要求引入重排或 LCEL。
- 下一步：进入 `P3.1`，为会话上下文增加可观测的 token 预算与裁剪策略。

### 13:45｜优化错别字与口语问题的 RAG 检索

- 所属阶段：`P2.6 业务规则 RAG` 补充优化。
- 完成内容：新增受控领域 Query 标准化；保留原 Query，仅在明确命中错别字或口语规则时增加标准化 Query，两路召回按 Chunk 合并并保留更高相关度。
- 数据模型：使用 `QueryNormalizationResult` 同时记录原 Query、标准化 Query 和实际应用的替换规则，避免不可解释地覆盖用户输入。
- 直接证据：4 条困难用例 Top1 从 `3/4` 提升到 `4/4`；重度错别字用例从错误知识修正为积分任务规则，口语用例相关度从 `0.3446` 提升到 `0.6014`。
- 验证证据：知识检索定向单测 `15/15`、真实困难检索 `4/4`；按照减少回归频率的约定，没有重跑 24 条完整检索、11 条 Agent 评测和全量测试。
- 工程边界：未实现通用拼写纠错、模型 Query Rewrite、混合检索或重排；领域词表只接收含义明确、可归纳的稳定表达。

### 14:16｜重排 Agent 项目最高优先级路线

- 完成内容：结合当前查询、RAG、受控兑换进度和 Agent 课程能力，重新定义项目主线；不再按课程顺序堆技术，而是先治理短期上下文，再落地兑换目标、长期偏好和积分规划。
- 技术准入：为长期记忆、复杂问题改写、混合检索、MCP 和多 Agent 分别增加直接证据门槛，并统一采用“业务场景 -> 当前基线 -> 失败样本 -> 定向 A/B -> 收益与复杂度比较”的决策闭环。
- 数据模型：路线图新增 `RedemptionGoal` 和 `UserPreference` 示例，明确只保存用户确认过的稳定目标与偏好；积分、库存、任务和订单等动态事实继续实时调用 Tool。
- 回归策略：落实“定向优先、里程碑全量”，减少小改动触发完整回归；纯文档调整不单独提交，随下一项相关功能一起提交。
- 课程覆盖变化：没有新增运行时能力，课程状态不变；课程 7 成为当前 P3 主线，课程 9 在 P4 做只读最小验证，课程 8 保持证据触发的 P6 候选项。
- 下一步：实现 `P3.1` token 可观测与确定性上下文裁剪，并用长会话用例验证关键业务状态不会丢失。

### 14:20｜扩展 Agent 产品蓝图与技术落点

- 完成内容：将产品从单一兑换顾问扩展为“用户增长顾问 + 运营智能工作台 + 增长知识中心 + 业务能力中心”，为课程技术建立可持续的真实业务载体。
- 业务扩展：用户侧增加兑换目标和积分规划；运营侧规划活动草案、规则审核、成本与库存风险分析、历史效果分析；所有活动发布与规则变更继续经过人工审核和确定性服务。
- 技术落点：长期记忆服务用户目标，复杂改写和高级 RAG 服务多约束活动策划，MCP 服务用户端、运营端和评测端复用，多 Agent 服务策划、分析与审核职责隔离。
- 决策原则：主动创造合理业务条件，但不预设复杂技术一定上线；每项技术仍需经过冻结基线和定向 A/B 验证收益。
- 课程覆盖变化：没有新增运行时实现，课程状态不变；课程 5、8、9 的后续落点从抽象候选调整为 P4-P6 的具体产品场景。
- 下一步：仍从 `P3.1` 开始，先完成 token 可观测和确定性裁剪，再建立用户目标与长期偏好模型。

### 14:36｜完成短期记忆窗口与会话 TTL 治理

- 所属阶段：`P3.1 短期记忆治理`。
- 完成内容：新增模型调用中间件，按 HumanMessage 划分完整轮次，并受 token 与轮次双预算限制模型可见历史；System Prompt、当前轮和当前待确认兑换优先保留。
- 状态边界：裁剪只影响本次模型输入，不删除 checkpointer 中 TTL 内的完整消息；待确认兑换从确认存储即时注入，确认凭证不会进入模型上下文。
- 可观测性：每次模型调用记录裁剪前后消息数、轮次数、估算 token、裁剪量和模型实际输入/输出 token；健康检查暴露预算、轮次和会话 TTL 配置。
- 生命周期：增加会话空闲 TTL，过期时删除检查点，并同步撤销该会话尚未使用的兑换确认凭证。
- 验证证据：上下文窗口、Agent、Runtime 和 Web 契约定向测试 `23/23` 通过，Python 编译检查通过；额外验证带中间件的真实 `create_agent` 可以正常编译。
- 课程覆盖变化：课程 7 增加 token 窗口、完整轮次裁剪、待确认状态保留、空闲 TTL 和 token 日志证据，状态保持“部分落地”。
- 下一步：进入 `P3.2`，建立兑换目标与长期偏好模型及其确认、修改、遗忘和过期机制。

### 15:31｜完成兑换目标与长期偏好

- 所属阶段：`P3.2 兑换目标与长期偏好`。
- 完成内容：新增 `growth-memory` Skill，支持目标和偏好的查询、准备新增/修改、准备遗忘；模型只能创建草稿，用户使用“确认保存/确认遗忘”后由 Runtime 在模型外确定性执行。
- 数据与边界：目标和偏好按用户存入独立 Redis 前缀，草稿按用户与会话隔离并自动过期；积分、库存、任务和订单等实时事实不入长期记忆；来源会话仅用于服务端审计。
- 工程增强：Redis 多实例使用事务监视和原子提交保证同一草稿最多应用一次；会话淘汰只撤销草稿，不删除已确认记录；上下文裁剪仍强制保留安全草稿摘要；前端新增保存与遗忘确认卡片。
- 验证证据：记忆、Skill、Runtime、上下文和 HTTP 定向回归 `32/32`；阶段性全量测试执行 `127` 项，`118` 项通过，`9` 项 Redis 集成测试按开关跳过；随后显式开启 Redis 集成测试，`9/9` 通过；前端生产构建通过。
- 课程覆盖变化：课程 7 调整为“已落地”，新增长期目标、稳定偏好、跨会话存储、显式确认、遗忘和过期治理证据；课程 10 增加第四个文件型 Skill。
- 下一步：进入 `P3.3`，让积分规划 Skill 读取已确认目标，并组合实时积分、任务和规则生成可更新计划。

### 15:59｜按课程原则修正长期记忆写入流程

- 所属阶段：`P3.2 兑换目标与长期偏好` 设计纠偏。
- 触发原因：复盘发现旧方案把受控兑换的二次确认机械复用到了全部记忆变更；第七讲实际要求是明确、稳定、可复用的信息可以写入，低置信度、冲突或敏感信息才需要额外处理。
- 完成内容：删除记忆草稿、过期配置、确认口令、Runtime 确认路由、HTTP 待确认字段和前端确认卡片；目标、偏好和遗忘改为用户明确表达后由结构化 Tool 校验并直接执行。
- 保留边界：闲聊和临时情绪不自动沉淀，偏好冲突先澄清，敏感与动态业务事实拒绝保存；Redis 用户隔离、来源会话、目标过期和兑换确认链路保持不变。
- 验证证据：记忆 Store、Skill、Runtime、上下文、HTTP 和 Skill 注册定向测试 `31/31`；全量单元测试执行 `123` 项，`114` 项通过、`9` 项 Redis 集成测试按开关跳过；随后单独启用长期记忆 Redis 集成测试 `2/2` 通过，前端生产构建通过。
- 课程覆盖变化：课程 7 的落地证据从“统一显式确认”修正为“明确意图直接写入 + 写入门槛 + 冲突、过期和遗忘治理”。
- 下一步：进入 `P3.3`，让积分规划 Skill 消费已保存目标并结合实时业务数据生成计划。

### 16:32｜修正自然偏好陈述的记忆触发规则

- 所属阶段：`P3.2 兑换目标与长期偏好` 手测问题修复。
- 触发原因：用户在两个会话中测试“我喜欢数码类商品”，新会话仍读取不到偏好。日志证明两次请求均为用户 `10`，读取结果为空且没有调用 `save_user_preferences`，排除了用户隔离、Redis 和服务版本问题。
- 根因：Prompt、Tool 和 Skill 把“用户明确表达”错误收窄为必须出现“请记住、以后按这个”等固定记忆指令，导致稳定偏好只作为本轮推荐条件。
- 完成内容：第一人称直接、稳定的目标或偏好陈述改为校验后直接写入，不再要求额外记忆口令；更新偏好前先读取旧记忆并合并。带“这次、今天、当前、先”等范围的临时条件仍不写入，也不根据点击、兑换和推荐选择推断偏好。
- 验证证据：Prompt 与 Skill 注册定向测试 `5/5` 通过；真实模型正向样本“我喜欢数码类商品”依次产生 `GROWTH_MEMORY_EMPTY -> USER_PREFERENCES_SAVED`，新会话读取返回 `GROWTH_MEMORY_FOUND`；反向样本“这次想看数码类商品”只调用奖品与积分查询，记忆统计保持为零。临时测试记忆已清理。
- 课程覆盖变化：课程 7 的“用户明确表达”从固定关键词判断修正为稳定语义判断，补充稳定陈述、临时条件和行为推断三类边界。
- 下一步：进入 `P3.3`，让积分规划 Skill 消费已保存目标并结合实时业务数据生成计划。
### 17:21｜完成原文优先长期记忆第一阶段

- 所属阶段：`P3.2.1 通用记忆内核`。
- 触发原因：固定目标与偏好 Schema 迫使 Agent 对“喜欢小鸟”“明年换手环”等明确表达追问奖品 ID、精确日期或分类，混淆了保存记忆与执行业务。
- 完成内容：新增原子 `MemoryItem`，忠实保留用户原文；结构化字段改为可选投影；增加 `remember_user_memory` Tool；由存储层执行 `ADD/UPDATE/SUPERSEDE/NOOP`，不再让模型读取并覆盖整份偏好。
- 持久化：新增 MySQL 表和迁移脚本，以同用户、同主题唯一生效记录保证冲突更新正确；旧版本保留为已被替代，明确遗忘删除对应内容。Redis 仅暂时保留为兼容后端。
- 验证证据：记忆 Store 与 Tool 定向测试 `11/11` 通过；本机 MySQL 完成宽泛偏好、冲突替代、不完整目标及查询闭环，结果为 `ADD -> SUPERSEDE -> ADD`，最终两条生效记忆，临时数据已清理。
- 下一步：完善记忆读取与按需召回，并在整个记忆模块完成后统一执行完整评测。

### 17:55｜完成长期记忆按需召回

- 所属阶段：`P3.2.2 长期记忆按需召回`。
- 触发原因：第一阶段读取接口会一次返回全部生效记忆，随着记忆增长会增加上下文成本，并让无关偏好干扰当前任务。
- 完成内容：为 `get_growth_memory` 增加当前问题、记忆类型、返回上限和显式全量参数；默认按类型、主题、原文相关性和更新时间召回少量原子记忆，只有用户明确查看全部时才返回完整记忆和旧聚合视图。
- 工程边界：当前记忆规模较小，先使用可解释的确定性检索，不引入额外向量服务；来源会话和来源消息仍不进入模型上下文。
- 验证证据：手环目标、泛化偏好、无关订单问题和显式全量四类关键边界通过；记忆 Store 与 Skill 定向测试 `15/15` 通过，完整回归按约定留到整个长期记忆模块完成后执行。
- 课程覆盖变化：课程 7 增加长期记忆按需召回、上下文注入上限和显式全量读取证据，状态保持“已落地”。
- 下一步：补齐 MySQL 事实源与 Redis 缓存的一致性边界、用户可查看和精确删除单条记忆的管理能力，再进入 `P3.3`。

### 18:08｜完成长期目标驱动的积分规划

- 所属阶段：`P3.3 积分规划 Skill`。
- 触发原因：长期目标已经可以保存和按需召回，但用户仍需重复指定奖品才能调用原积分规划，记忆尚未形成业务闭环。
- 完成内容：新增 `plan_points_for_saved_goal` 组合 Tool；读取当前用户目标，处理多目标和过期目标，优先使用已绑定奖品 ID，宽泛目标按主题匹配当前奖品，唯一匹配后复用现有 `PointsPlanningSkill` 查询实时资格、积分缺口和任务。
- 确定性边界：没有目标时不访问业务服务；目标或奖品不唯一时返回候选让用户选择；积分、库存、资格和任务不从记忆读取；任务缺少周期数据时不虚构完成日期。
- 验证证据：新增目标规划与原积分规划定向测试 `14/14` 通过，覆盖 Store 共享、唯一匹配、多目标、多奖品候选、过期停止和实时查询；连同记忆、Prompt 与 Skill 注册的本轮定向验证 `34/34` 通过，完整回归按约定未执行。
- 课程覆盖变化：课程 7 增加长期目标消费闭环，课程 10 的积分规划 Skill 升级为 `1.1.0`，课程 12 增加跨会话目标到实时计划的端到端业务链路。
- 下一步：进入 `P3.4`，建立复杂规划评测，先验证现有单 Agent 在多约束目标下的表现，再决定是否增加确定性拆解或 Query Rewrite。

### 19:20｜完成复杂规划评测与任务约束契约

- 所属阶段：`P3.4 复杂规划评测`。
- 完成内容：新增 8 条带隔离长期记忆种子的规划 Fixture；评测 Runner 增加 Agent Tool 与后端调用次数检查；积分规划 Tool 支持允许和排除任务名称，Skill 在一次实时任务查询后确定性过滤。
- 基线证据：P01 为解析“不要分享”先查任务，再由组合 Skill 重复查任务，共 2 个 Agent Tool、2 次任务查询和 3 次模型调用；P08 虽给出正确任务，但允许集合没有进入 Tool 参数，属于偶然正确。
- 修复证据：P01 修复后降为 1 个组合 Tool、1 次任务查询和 2 次模型调用；P02 至 P07 修改后为 `6/6`，P01、P08 最终定向复验为 `2/2`；相关定向单元测试通过，未执行项目全量回归。
- 评测修正：参数“分享”和“分享活动”都能被 Skill 正确匹配，因此评分器允许受控的等价参数形态；P08 遗漏“月底”属于真实回答缺口，通过通用期限规则修复。
- 课程覆盖变化：课程 2 增加期限忠实表达证据，课程 3 增加任务约束 Tool Schema，课程 7 完成目标到计划的复杂消歧闭环，课程 11 增加隔离记忆 Fixture 与调用次数评分。
- 技术决策：现有失败由 Tool 契约解决，没有证据支持在用户积分规划中引入 Query Rewrite、LCEL 或多 Agent。
- 下一步：进入 `P4.1`，定义运营活动草案、用户群、预算、任务、奖品与风险模型，并先建设只读 Tool。

### 20:31｜完成运营活动草案第一阶段

- 所属阶段：`P4.1.1 活动草案模型与确定性规划 Skill`。
- 触发原因：现有 Agent 只有普通用户身份，若直接加入运营 Tool，会混淆用户服务和运营策划的权限边界；同时当前尚无真实运营数据接口，不能让模型自行补全用户规模、库存或参与率。
- 完成内容：新增活动简报、用户群、任务、奖品、历史指标和活动草案模型；增加 `CampaignDataProvider` 只读数据契约、`campaign-planning` Skill 和独立 `build_operator_tools`；草案包含成本上限、风险和来源快照，固定为可编辑、不可直接发布且必须人工审核。
- 确定性边界：缺历史参与率时返回待补数据，快照超过 24 小时时停止生成，预算放不下任何任务时返回约束冲突；奖品只按活动窗口、库存和可用性筛选，目标匹配度明确留给人工审核。
- 权限边界：运营 Tool 要求活动读取与草案权限，且没有注册进普通用户 Agent；当前 `/v1/chat` 的 Tool 集合保持不变。
- 验证证据：活动草案、缺指标、过期快照、预算冲突、运营 Tool 和权限不足等定向测试，连同 Skill 注册测试共 `10/10` 通过；Python 编译检查和差异格式检查通过，按减少回归频率的约定未执行全量回归。
- 课程覆盖变化：课程 1 增加用户端与运营端边界证据；课程 3 增加独立运营 Tool Schema 和权限前置检查；课程 10 增加第五个文件型 Skill；课程 12 增加运营草案最小切片。
- 剩余问题：尚未建立可信运营令牌和独立 HTTP 入口，`CampaignDataProvider` 尚未接入真实只读数据源，也尚未建立运营 Fixture 与完整评测指标。
- 下一步：进入 `P4.1.2`，先建立运营身份和独立 API 入口，再接用户群摘要、任务规则、奖品库存和历史活动指标数据。

### 21:12｜接入可信运营入口与真实只读快照

- 所属阶段：`P4.1.2 可信运营身份与真实数据适配`。
- 完成内容：新增与普通用户令牌隔离的运营访问令牌，运营 ID 和 `campaign:read`、`campaign:draft` 权限由服务端配置；开放独立快照和草案 API，但没有把运营 Tool 注册进 `/v1/chat`。
- 数据事实：Java 新增运营只读快照接口；`ALL_USERS` 和 `POINTS_AT_LEAST_500` 由 `user_currency` 实时计算，任务读取 `task_config`，普通奖品库存读取 `award_inventory_split` 总和，可超卖奖品读取配置库存。无法计算的自然语言客群会被拒绝。
- 历史指标：新增统一的 `campaign_metric_history` 表。正式迁移只建空表，演示初始化数据带有明确的 `demo_campaign_report` 来源；没有参与率时草案返回待补数据，不自行猜测。
- 验证证据：Java 运营快照定向测试 `3/3`，连同既有查询服务共 `8/8`；Python 活动规划、运营 API、业务客户端与既有 Web 契约定向测试 `21/21`；验证普通用户令牌隔离、服务端权限拒绝、不可发布草案、任务时间窗口和 Java 响应模型校验。按约定未执行全量回归。
- 课程覆盖变化：课程 1 增加用户端与运营端 API/身份边界；课程 3 补齐独立运营身份和服务端权限；课程 12 增加真实客群、任务、库存、历史指标到草案的端到端闭环。
- 剩余问题：历史指标尚无活动结束后的自动回写链路；当前业务数据不能支持未登录客群；尚未建立正式运营 Fixture 和完整指标。
- 下一步：进入 `P4.1.3`，建立运营 Fixture 与评测基线。

### 22:05｜建立运营活动草案评测基线

- 所属阶段：`P4.1.3 运营 Fixture 与正式基线`。
- 完成内容：冻结 8 条运营场景，覆盖完整草案、只读权限、缺少读取权限、未知客群、缺历史指标、过期快照、预算冲突和候选数量上限；新增不调用模型的确定性 Runner，分别统计权限隔离、数据引用正确性、约束满足率和草案完整性。
- 首轮修正：第一次运行只有 `5/8`，原因是 Fixture 顶层历史指标字段使用了错误命名，导致三个场景被解析为空指标；保留排查结论并修正数据契约，没有修改业务逻辑或降低评分标准。
- 验证证据：最终用例 `8/8`；权限隔离 `16/16`、数据引用 `21/21`、约束满足 `32/32`、草案完整性 `20/20`。主动篡改任务来源和预计成本后，评分器均能识别失败；运营相关定向测试 `15/15` 通过，按减少回归频率的约定未执行全量回归。
- 课程覆盖变化：课程 11 增加运营草案四维确定性评分证据；课程 12 补齐运营身份、真实快照、确定性草案到冻结验收基线的最小闭环；`P4.1` 完成。
- 剩余问题：当前基线不调用模型，不衡量运营自然语言路由、Token、措辞和多轮稳定性；历史指标仍未自动回写。
- 下一步：进入 `P4.2.1`，建立最小运营知识集和对应冲突用例，为高级 RAG 验证准备真实业务材料。

### 23:05｜拆分活动金额预算与积分发放约束

- 所属阶段：`P4.1.4 真实金额预算与积分发放双约束`。
- 触发原因：原模型把 `budget_points` 当作活动预算，但积分只能表达用户侧激励，不等于企业采购奖品的真实金额。
- 完成内容：活动简报改为金额预算 `budget_amount_cents` 和积分发放上限 `points_issuance_cap`；奖品配置新增单位成本 `unitCostCents`；草案分别输出预计发放积分、奖品计划数量和计划金额。
- 计算边界：兑换积分只表示用户门槛，单位成本来自采购或运营事实，两者不做固定换算；缺少单位成本时返回 `AWARD_UNIT_COST_MISSING`，不生成虚假金额。
- 数据变更：新增可单独执行的数据库迁移，并为初始化和本地演示数据补充明确的单位成本；本地 `budou` 已完成字段迁移和演示成本回填。
- 验证证据：Python 活动规划、评测和运营 API 定向测试 `17/17`；Java 快照与用户查询测试 `8/8`；冻结活动用例 `8/8`，约束满足率 `35/35`。按减少回归频率的约定未执行全量回归。
- 课程覆盖变化：课程 3 补充金额与积分的独立 Tool Schema；课程 10 补充双约束 Skill；课程 11 的成本评分升级为积分和金额分别重算；课程 12 增加数据库事实到运营草案的金额闭环。
- 剩余问题：当前奖品组合按低单位成本优先覆盖预计参与者，活动目标相关性仍由人工审核；真实环境必须由采购或运营系统回填单位成本。
- 下一步：按路线图进入 `P4.2.1`，建立最小运营知识集和知识冲突用例。

### 23:52｜建立运营知识底座与受众隔离

- 所属阶段：`P4.2.1 运营知识扩容与治理元数据`。
- 完成内容：新增运营制度、奖品字段说明、演示活动复盘、异常处理手册和规则变更通知；知识目录由 3 篇扩为 8 篇，共 33 个 Chunk。
- 治理能力：全部知识增加业务类型、适用受众、生效时间、可选失效时间和权威级别；这些字段从 Front Matter 进入 Chunk 和检索结果，为后续冲突裁决提供依据。
- 安全边界：检索服务由服务端固定 `allowed_audiences`，普通用户只读取 `end_user` 知识，不能通过模型参数访问 `operator` 手册；演示复盘明确标注为 Fixture，不冒充生产经营数据。
- 验证证据：目录、切分和检索定向测试 `26/26`；真实 Embedding 重建目录版本 `2026.08.22.2`；精确手册编号、奖品成本语义和历史案例三个抽查查询的 Top1 均正确，相关度约为 `0.65`、`0.62`、`0.75`。
- 课程覆盖变化：课程 5 增加多类型运营知识、治理元数据和受众隔离；课程 12 增加运营草案到知识支持的基础闭环。
- 剩余问题：尚未冻结运营 RAG 困难集，也未实现知识冲突裁决、关键词检索、混合召回和重排。
- 下一步：进入 `P4.2.2`，冻结精确编号、精确条款、语义案例、口语问题与知识冲突用例，并记录纯向量检索基线。

### 00:20｜冻结运营 RAG 用例并建立纯向量基线

- 所属阶段：`P4.2.2 运营 RAG 冻结集与基线`。
- 完成内容：扩展 RAG Runner，使其可指定用例文件和服务端受众；冻结 15 条运营检索用例，覆盖精确编号、精确条款、语义案例、字段语义、口语表达、知识冲突、受众隔离和无关问题。
- 基线结果：纯向量检索 `15/15`，Top1 `100%`、Hit@3 `100%`、负样本拒答率 `100%`、引用结构有效率 `100%`；正样本 Top1 平均分 `0.6034`，最低分 `0.4214`。
- 直接证据：受众隔离负样本的原始最高候选分为 `0.6413`，但因候选属于用户知识而被运营检索过滤；口语快照问题只有约 `0.12` 阈值余量，是扩容后重点观察项。
- 验证证据：Runner 与检索定向测试 `25/25`；原始结果保存为 `operator_rag_vector_baseline_20260822.json`。按约定没有调用对话模型，也没有执行项目全量回归。
- 技术决策：当前 8 篇、33 个 Chunk 下没有证据支持立即引入混合检索、重排或 Query Rewrite；保留纯向量模式作为后续消融基线。
- 下一步：进入 `P4.2.3`，实现运营检索 Tool、生效时间过滤和真正的多文档冲突治理。
