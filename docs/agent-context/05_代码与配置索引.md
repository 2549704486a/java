# 代码与配置索引

## Agent 最小实现

| 内容 | 位置 |
| --- | --- |
| Agent 启动入口 | `agent-service/app/main.py` |
| Agent 构建与运行 | `agent-service/app/agent.py` |
| System Prompt | `agent-service/app/prompt.py` |
| Function Calling Tool | `agent-service/app/tools.py` |
| Java 业务接口客户端 | `agent-service/app/api_client.py` |
| 积分规划 Skill | `agent-service/app/skills/points_plan.py` |
| Skill 声明 | `agent-service/skills/points-planning/SKILL.md` |
| 离线测试 | `agent-service/tests/` |

当前 Agent 只调用 `/agent/query/**`，不调用兑换和任务写接口，也不直连数据库及中间件。

> 不要为了回答一个局部问题读取整个仓库。先在这里按问题找到最小文件集合，再打开源码核验。

## 1. 秒杀主链路源码

| 问题 | 读取文件 |
| --- | --- |
| 接口和返回 | `controller/UserAwardController.java`、`utils/ResultCodeEnum.java` |
| 入口校验和结果查询 | `service/impl/UserAwardServiceImpl.java` |
| 事务消息发送 | `service/impl/TransactionServiceImpl.java`、`infra/TransactionProducer.java` |
| 半消息本地事务和回查 | `infra/TransactionProducerConfig.java` |
| 消费、分片、重试 | `infra/TransactionConsumer.java`、`config/OldChainRetryProperties.java` |
| 消费数据库事务 | `service/impl/ConsumerServiceImpl.java` |
| 普通消费者配置 | `infra/TransactionConsumerConfig.java` |
| 死信处理 | `infra/DeadLetterConsumer.java`、`infra/DeadLetterConsumerConfig.java` |
| Redis 操作和键定义 | `dao/redis/RedisDao.java`、`utils/SeckillRedisKeys.java` |
| 缓存和预热 | `utils/CacheConfig.java`、`utils/CacheWarmer.java` |
| Canal 同步 | `infra/RDSBinlog.java`、`config/CanalClientProperties.java` |
| 最小观测 | `utils/SeckillObservability.java` |

以上路径都位于 `incentive/src/main/java/com/budou/incentive/`。

## 2. Agent 只读查询源码

| 问题 | 读取文件 |
| --- | --- |
| HTTP 工具入口 | `controller/AgentQueryController.java` |
| 查询编排和资格判断 | `service/AgentQueryService.java` |
| 稳定工具返回结构 | `dto/agent/AgentToolResponse.java` |
| 工具 DTO | `dto/agent/UserPointsView.java`、`TaskOptionView.java`、`AwardDetailView.java`、`AwardOptionView.java`、`ExchangeEligibilityView.java` |
| 数据查询 | `UserCurrencyMapper`、`TaskConfigMapper`、`FinishTaskRecordMapper`、`AwardConfigMapper`、`UserAwardMapper` |
| 产品和工具设计 | `docs/agent-design/` |

## 3. 数据库源码

- 完整初始化：`sql/init_budou.sql`
- 现存库修复：`sql/repair_current_budou.sql`
- 调整库存分片：`sql/repartition_award_inventory_split.sql`
- 秒杀 Mapper：`UserAwardMapper`、`IdempotentMapper`、`AwardInventorySplitMapper`、`UserCurrencyMapper`、`AwardConfigMapper`

读取 SQL 时只提取表结构、约束和初始化关系，不引入文件注释里的压测目的或历史数据。

## 4. 配置入口

| 配置组 | 文件 | 关键环境变量 |
| --- | --- | --- |
| MySQL | `application-dev.properties`、`application-prod.properties` | `SPRING_DATASOURCE_URL`、`USERNAME`、`PASSWORD` |
| Redis | 同上 | `SPRING_DATA_REDIS_HOST`、`PORT`、`USERNAME`、`PASSWORD` |
| RocketMQ | `application.properties` 和环境配置 | `ROCKETMQ_NAME_SERVER`、Topic、生产者组、消费者组 |
| Canal | `application.properties` | `CANAL_HOST`、`PORT`、订阅表达式、批量大小、轮询间隔 |
| 缓存预热 | `application.properties` | `CACHE_WARM_*` |
| 旧链路重试 | `application.properties` | `SECKILL_OLD_CHAIN_*` |

配置文件只保存占位符和本地默认值，Agent 不应把密码写入 Prompt、日志或代码。

## 5. 当前默认重试配置

- 线程内探测轮数：2
- 退避列表：5ms、15ms
- 分片锁过期：10 秒
- 自定义延迟重试：开启
- 自定义延迟重试上限：6
- 延迟级别：`1,1,2,2,3,4`
- RocketMQ 原生最大重试次数：3

延迟级别对应的真实时长由 Broker 配置决定，不能只根据数字自行假设。

## 6. 构建与验证

```powershell
cd D:\工作\incentive-事务消息\incentive
mvn -q -DskipTests package
mvn -q -Dtest=AgentQueryServiceTest test
```

现有 Spring 集成测试会连接 MySQL、Redis、RocketMQ 和 Canal。没有准备这些依赖时，`mvn test` 失败不能直接归因于业务代码。

## 7. 不应误加载的源码

- `InventoryDemoController` 和 `InventoryDemoServiceImpl` 是 Redis Lua 演示，不是主链路。
- `TestController` 已被注释。
- `ops/` 不属于本上下文包。
