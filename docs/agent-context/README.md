# 项目上下文导航（按需）

本目录不是启动必读项。默认只遵循根目录 `AGENTS.md`，再定位当前问题的源码；已有上下文够用时无需读取本页或其他说明。

| 具体疑问 | 可选文档，只读对应章节 |
| --- | --- |
| 产品能做什么 | 根目录 `PRODUCT.md` |
| 找不到代码入口 | `05_代码与配置索引.md`，先核对实际文件是否存在 |
| 兑换事务调用过程 | `03_旧秒杀链路.md` |
| 缓存与落账职责 | `04_数据状态与一致性.md` |
| 某个已知约束或风险 | `06_已知约束与待确认问题.md` |
| Agent 业务权限边界 | `07_Agent接入边界.md` |
| 当前复杂需求的验收 | 当前 `openspec/changes/<change>/` 中对应规格 |

源码和可复现证据优先于文档。链接仅供定位，不代表要求继续读取；不通读整个目录或所有设计文档。

路线图、课程索引及所有课程内容默认不加载、不更新。只有用户本次明确要求加载课程时才读取指定内容；通用技术问题无需加载课程。任务完成后不自动同步课程覆盖表和日记。
