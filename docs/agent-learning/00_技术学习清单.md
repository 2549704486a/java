# Agent 技术学习清单

> 这份清单从当前代码、依赖和测试反向生成，用来防止“功能完成了，但关键技术没有学到或没有进入最终总结”。它是学习进度的事实来源，不写入业务 Agent 的 Prompt。

## 1. 状态说明

| 状态 | 含义 |
| --- | --- |
| 已实践 | 已经运行或测试过，但不代表原理已经讲清楚 |
| 初步理解 | 已能结合当前调用链说明用途，仍可继续深入 |
| 待学习 | 当前代码已经使用，后续需要结合代码专题讲解 |
| 待实现 | 设计中已经明确，但当前代码尚未实现 |

## 2. 当前技术总账

| 分类 | 技术点 | 在项目中的作用 | 代码或证据 | 当前状态 | 总结去向 |
| --- | --- | --- | --- | --- | --- |
| Agent 框架 | LangChain `create_agent` | 建立“模型选择工具 -> 执行工具 -> 继续推理”的 Agent 循环 | `agent-service/app/agent.py` | 已实践，待学习 | 工程总结、面试文档 |
| 模型接入 | `ChatOpenAI` 与 OpenAI 兼容接口 | 配置模型、API Key、超时、温度和模型侧重试 | `agent-service/app/agent.py`、`config.py` | 已实践，待学习 | 工程总结 |
| Prompt | System Prompt | 约束角色、动态事实来源、只读边界和回答格式 | `agent-service/app/prompt.py` | 已完成首轮基线、调优与回归 | 工程总结、面试文档 |
| Function Calling | Tool 名称、描述与参数 Schema | 让模型以结构化参数选择 Java 查询或积分规划能力 | `agent-service/app/tools.py` | 已实践，待学习 | 工程总结、面试文档 |
| 数据契约 | Pydantic `BaseModel` | 将 Java JSON 校验为稳定的 Python 业务对象 | `agent-service/app/models.py` | 待学习 | 专题笔记、工程总结、面试文档 |
| 数据契约 | `Field(alias=...)`、`Literal` | 适配 Java 驼峰字段并限制业务状态取值 | `agent-service/app/models.py` | 待学习 | Pydantic 专题 |
| 数据契约 | `model_validate()`、`ValidationError`、`model_dump()` | 校验工具输入输出，格式异常时停止规划而不是猜测 | `models.py`、`points_plan.py`、`tools.py` | 待学习 | Pydantic 专题、工程总结 |
| 身份边界 | 闭包绑定 `user_id` | 用户身份由运行时注入，不允许模型自行指定其他用户 | `agent-service/app/tools.py` | 初步理解，待学习 | 安全专题、面试文档 |
| 写操作授权 | 一次性确认凭证与确定性状态机 | 将用户确认绑定到用户、会话、奖品和有效期，不把对话记忆当作执行授权 | `app/confirmation_store.py`、`docs/agent-design/19_受控兑换Skill实现.md` | 已实践，待学习 | 安全专题、工程总结、面试文档 |
| 并发与幂等 | 原子凭证消费 | 通过同一把锁保护检查与状态更新，确保重复或并发确认最多触发一次 Java 写调用 | `app/confirmation_store.py`、`tests/test_controlled_exchange.py` | 已实践，待学习 | 并发专题、工程总结 |
| 能力分层 | Tool 与 Skill 分层 | 基础 Tool 查询单项事实，组合 Skill 编排多个查询与规则 | `tools.py`、`skills/points_plan.py`、`skills/points-planning/SKILL.md` | 已实践，初步理解 | 工程总结、面试文档 |
| 能力分层 | Skill 注册与渐进披露 | 解析并校验 SKILL.md，只向模型暴露精简触发信息，激活时记录版本和哈希 | `skills/registry.py`、`docs/agent-design/09_Skill运行时标准化.md` | 已实践，待学习 | Skill 专题、工程总结、面试文档 |
| 服务化 | FastAPI 与 Pydantic 请求契约 | 将 CLI Agent 暴露为 `/v1/chat`，在进入业务前校验用户 ID 和消息 | `app/web.py`、`tests/test_web.py` | 已实践，待学习 | FastAPI 专题、工程总结 |
| 生命周期 | 共享连接与有界 Agent 缓存 | 按用户隔离 Tool 身份，复用业务 HTTP 连接，通过 LRU 防止缓存无限增长 | `app/runtime.py`、`tests/test_runtime.py` | 已实践，待学习 | 服务化专题、面试文档 |
| 可观测性 | 请求 ID 与错误脱敏 | 使用请求 ID 串联日志，服务端保留堆栈，对调用方返回稳定错误码 | `app/web.py`、`app/logging_config.py` | 已实践，待学习 | SRE 专题、工程总结 |
| 可观测性 | Tool/Skill 结构化执行轨迹 | 按请求记录实际工具参数、完成状态、结果码和耗时，并与模型声明及后端路径对照 | `app/trace.py`、`evals/runner.py`、`docs/agent-design/14_Tool调用链可观测性.md` | 已实践，待学习 | 可观测性专题、面试文档 |
| 记忆系统 | Checkpointer 与 thread_id | 使用 InMemorySaver 保存单次会话历史，通过用户 ID 与会话 ID 组合隔离上下文 | `app/agent.py`、`app/runtime.py`、`docs/agent-design/11_短期记忆与会话隔离.md` | 已实践，初步理解 | 记忆专题、面试文档 |
| 记忆治理 | 会话锁与会话 LRU | 串行处理同一会话的消息，淘汰空闲旧会话并删除检查点 | `app/runtime.py`、`tests/test_runtime.py` | 已实践，待学习 | 并发专题、工程总结 |
| 业务编排 | 确定性决策树 | 根据资格原因码决定立即成功、阻断或继续查询任务 | `agent-service/app/skills/points_plan.py` | 已实践，待逐分支阅读 | 工程总结 |
| 业务编排 | 确定性奖品推荐 | 编排积分与奖品查询，过滤不可兑换奖品并执行稳定排序 | `app/skills/award_recommendation.py`、`skills/award-recommendation/SKILL.md` | 已实践，待学习 | Skill 专题、工程总结 |
| 算法 | 任务组合与最小覆盖 | 优先待领取奖励，再按任务数、超额积分和任务 ID 选择方案 | `PointsPlanningSkill._smallest_cover()` | 已实践，待学习 | 算法专题、面试文档 |
| 服务通信 | HTTPX 同步客户端 | Agent 仅通过 `/agent/query/**` 获取动态业务事实 | `agent-service/app/api_client.py` | 已实践，待学习 | 工程总结 |
| 稳定性 | 超时、有限重试与指数退避 | 只重试网络异常及 `408/429/5xx`，避免业务错误被盲目重试 | `BusinessApiClient._get()`、`_backoff()` | 已实践，待学习 | 稳定性专题、面试文档 |
| 稳定性 | 非幂等写入的未知结果 | POST 超时或连接中断时区分失败与未知，禁止模型和 Tool 自动重放 | `app/api_client.py`、`app/skills/controlled_exchange.py` | 已实践，待学习 | 稳定性专题、面试文档 |
| 错误模型 | `ToolEnvelope` 与 `retryable` | 统一 Java 业务码、HTTP 异常和 Agent 可见错误语义 | `models.py`、`api_client.py` | 初步理解，待学习 | 工程总结 |
| 配置 | `python-dotenv`、不可变 `Settings` | 从本地 `.env` 注入地址、模型和超时，不提交真实密钥 | `agent-service/app/config.py`、`.env.example` | 已实践 | 工程总结 |
| 可观测性 | Python 日志与分段耗时 | 记录业务接口状态、重试次数和 Skill 总耗时 | `main.py`、`api_client.py`、`logs/agent-service.log` | 已实践，待学习 | 工程总结 |
| 测试 | `unittest`、Fake Client | 隔离 Java 服务验证 Skill 的业务分支和任务组合 | `agent-service/tests/test_points_plan.py` | 已实践，待学习 | 测试专题、面试文档 |
| 测试 | HTTPX `MockTransport` | 在无真实网络时验证瞬时错误重试 | `agent-service/tests/test_api_client.py` | 已实践，待学习 | 测试专题 |
| 会话状态 | 短期记忆 | 使用 Checkpointer 保存同一会话的消息历史，使省略奖品 ID 等多轮表达仍可被理解 | `agent-service/app/agent.py`、`app/runtime.py`、`evals/multiturn_runner.py` | 已实践，初步理解 | 记忆专题、工程总结 |
| 评测 | Agent 端到端评测 | 验证工具选择、参数提取、事实一致性和只读边界 | `agent-service/evals/`、`docs/agent-design/07_第一轮Agent评测报告.md` | 已完成第一轮 | 评测报告、面试文档 |
| 评测 | 分层混合评分与数据集治理 | 组合代码评分、结果状态、轨迹、LLM Judge、人工校准、盲测和线上回流 | `docs/agent-design/08_Agent评测体系升级.md` | 已完成方案，待实现 | 工程总结、面试文档 |
| 评测 | 高风险写操作安全评测 | 分别验证无授权不写、重复确认只写一次、身份隔离和未知状态不重试 | `tests/test_confirmation_store.py`、`test_controlled_exchange.py`、`evals/results/controlled_exchange_closure_full_20260821.json` | 确定性测试与模型回归已实践 | 测试专题、面试文档 |
| 缓存一致性 | Canal binlog 同步与缓存键契约 | 将旧链路提交后的 MySQL 积分、分片库存变化同步到 Redis，并用测试约束读写双方键名一致 | `RDSBinlog.java`、`RDSBinlogTest.java`、`docs/agent-design/19_受控兑换Skill实现.md` | 已实践，待学习 | 一致性专题、工程总结 |
| 部署 | 常驻 Web 服务 | 避免每次 CLI 冷启动，并向页面提供稳定 Agent API | `agent-service/app/web.py`、`app/server.py`、`docs/agent-design/10_Agent服务化.md` | 已实践，待学习 | 服务化专题、部署文档 |
| 前端工程 | React、TypeScript 与 Vite | 实现奖品中心、用户切换和 Agent 会话界面，并在构建期校验类型 | `web-ui/src/`、`web-ui/vite.config.mjs` | 已实践，待学习 | 前端工程专题、工程总结 |
| 接口分层 | 页面聚合接口 | 在 FastAPI 聚合积分和奖品，隔离浏览器与 Java 信封协议 | `agent-service/app/web.py` 的 `/v1/dashboard/{user_id}` | 已实践，待学习 | BFF 专题、工程总结 |
| 前后端部署 | 同源静态托管与开发代理 | 开发时由 Vite 代理 API，生产时由 FastAPI 托管静态资源，避免额外 CORS | `vite.config.mjs`、`app/web.py`、`start-local.ps1` | 已实践，待学习 | 部署专题、工程总结 |

## 3. 推荐学习顺序

1. 沿一次自然语言请求读完 `main.py -> agent.py -> tools.py -> points_plan.py -> api_client.py`。
2. 在阅读 `models.py` 时专题学习 Pydantic，理解数据如何进入和离开 Agent。
3. 结合一次真实工具调用学习 Function Calling，以及 Tool 描述如何影响模型选择。
4. 阅读积分规划分支和任务组合算法，区分模型推理与确定性业务计算。
5. 结合现有单元测试学习 Fake、MockTransport、异常路径和回归保护。
6. 结合已完成的 HTTP 服务学习 FastAPI 生命周期、同步路由线程池和 Agent 缓存边界。

## 4. 当前进度

- 已完成：环境准备、Java 查询接口联通、Skill 离线测试、Skill 真实调用、自然语言 Agent 运行。
- 已完成：第一轮 15 个固定场景、2 个真实集成用例、Prompt 单变量调优与全量回归。
- 已完成：积分规划 `SKILL.md` 的运行时注册、声明校验、精简元信息暴露和版本追踪。
- 已完成：Agent FastAPI 服务化、按用户隔离的有界缓存、请求追踪和错误脱敏。
- 已完成：基于 InMemorySaver 的进程内短期记忆、会话隔离和多轮自动化评测。
- 已完成：第二个业务 Skill，支持按实时积分和资格推荐奖品，并完成多 Skill 路由回归。
- 已完成：请求级 Tool/Skill 结构化执行轨迹、`request_id` 日志关联和基于实际执行的多 Skill 评测，固定集回归 `22/22`。
- 已完成：受控兑换 Skill 的服务端确定性确认、凭证隐藏、页面确认卡片、Java 旧链路适配和真实兑换闭环。
- 已完成：React 奖品中心与 Agent 对话页面、dashboard 聚合接口、同源静态托管和受控兑换入口。
- 正在进行：理解评测证据和 `agent-service` 完整调用链；评测升级方案已沉淀，尚未实施。
- 当前待展开：Pydantic 如何校验 Java 返回数据和 Tool 参数。
- 下一工程阶段：修正剩余模型路由/措辞回归；多实例前将确认存储迁移到共享存储，并在 Java 端真正使用 `Idempotency-Key` 做持久化幂等。
