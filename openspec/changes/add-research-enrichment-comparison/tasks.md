## 1. 联网 Spike 与冻结契约

- [x] 1.1 在当前虚拟环境验证一个无密钥搜索实现和受限 `httpx` 读取能分别取得公开商品页、活动规则页、客群方法页的正文，记录成功 URL、失败类别和获取时间到 `docs/agent-design/63_公网研究建库与多Agent对照.md`；若自动搜索不稳定，验证显式 URL 降级链路并停止宣称自动发现可用
- [x] 1.2 在 `app/research/models.py` 定义研究简报、研究计划、来源证据、逐条结论、候选资产、运行摘要、批准清单和评分契约，并新增一份 `PILOT` 与三份 `OFFICIAL` 简报实例；运行定向模型测试验证必填字段、枚举、数量边界和禁止扩展字段
- [x] 1.3 冻结四臂实验的来源调用预算、评分表和多 Agent 保留规则，校验三份正式简报均能被同一加载器解析，并将本组规划与契约改动使用中文提交且不暂存现有 MCP 文件

## 2. 公网证据与确定性治理

- [x] 2.1 实现单一公网搜索/读取模块并固定通过 Spike 的依赖版本，对每次请求和跳转执行协议、凭据、公网地址、响应大小、内容类型、跳转次数和超时限制；使用 `httpx.MockTransport` 定向验证正常页面、私网地址、恶意跳转、超大正文、非文本和超时
- [x] 2.2 将搜索结果与实际读取证据分开，输出标题、发布者、获取时间、短摘录和读取状态，并把网页正文包装为不可信数据；定向验证搜索摘要不能单独成为证据且页面提示注入不能获得额外 Tool
- [x] 2.3 实现运行目录隔离和不可覆盖写入，以及 Schema、来源引用、重复候选、结论状态、数量边界和禁止业务事实门禁；验证部分失败可被逐条报告、正式运行不可覆盖且不会修改 MySQL、活动状态或 `knowledge/manifest.json`
- [x] 2.4 更新 `63` 号设计文档的数据模型和 Spike 事实，执行本组研究模型、公网边界与门禁测试后，以中文提交本组代码和文档，不执行无关全量回归

## 3. 项目单 Agent 基线

- [x] 3.1 实现只拥有公网搜索与读取 Tool 的单 Agent 研究器，使用统一结构化候选契约并记录最小运行摘要；通过假模型和假 Tool 验证来源调用、结构化输出、用量缺失语义和业务写 Tool 不可见
- [x] 3.2 增加 `run --arm PROJECT_SINGLE` 命令并用 `PILOT` 简报执行一次真实模型研究，核对来源实际可读、候选可追溯、门禁结果和错误阶段；仅修正可复现问题并保留 Pilot 证据
- [x] 3.3 将单 Agent 基线的真实流程、关键轨迹和未验证范围补入 `63` 号文档，运行单 Agent 定向测试后以中文提交本组代码和文档

## 4. 项目多 Agent 候选流程

- [x] 4.1 实现规划 Agent，将同一简报约束为最多三个互不重叠的结构化研究任务；使用假模型验证越界任务、重复任务、空计划和模型解析失败均在公网访问前被拒绝
- [x] 4.2 实现确定性协调器、独立上下文研究 Agent、并行汇合、证据审核 Agent 和最多一次定向修订；验证角色 Tool 最小化、并行失败可定位、审核不能覆盖门禁且运行摘要完整累计各阶段调用
- [x] 4.3 增加 `run --arm PROJECT_MULTI` 命令，用与单 Agent 相同的 `PILOT` 简报、模型、来源预算和 Schema 运行一次 A/B；根据 Pilot 只调整尚未冻结的提示与字段，然后冻结正式版本并记录差异
- [x] 4.4 更新 `63` 号文档中的多 Agent 调用图、结构化状态实例和 Pilot 结果，运行多 Agent 工作流关键测试后以中文提交本组代码和文档

## 5. 四臂正式对照

- [ ] 5.1 实现外部执行臂结果导入、统一门禁统计、匿名材料、评分记录锁定、揭盲和比较报告；用四份小型固定结果验证缺席执行臂、未知 Token、匿名映射隔离和预声明保留规则
- [ ] 5.2 使用冻结简报完成 `CODEX_DIRECT` 三份正式研究，执行期间不显式调用子 Agent，将结构化结果和可观测成本导入隔离目录并通过统一门禁
- [ ] 5.3 使用相同冻结简报完成 `CODEX_DELEGATED` 三份正式研究，显式委派互不依赖的研究任务，将结构化结果和可观测成本导入隔离目录并通过统一门禁
- [ ] 5.4 使用相同冻结简报依次完成 `PROJECT_SINGLE` 与 `PROJECT_MULTI` 的六份正式研究，确认两组项目运行使用相同模型、搜索实现、来源预算、Schema 和门禁，并保留失败结果而不人工补齐
- [ ] 5.5 生成不含执行臂身份的评分材料，邀请用户完成三份简报的四维评分后锁定评分、揭盲并生成报告；报告分别解释项目内部架构比较和项目对 Codex 的实际效果比较

## 6. 保留决策与候选导出

- [ ] 6.1 按冻结规则得出 `KEEP_MULTI_AGENT`、`KEEP_SINGLE_AGENT` 或 `INCONCLUSIVE`，并执行对应代码收敛：未证明收益时移除多 Agent 运行入口但保留单 Agent、研究产物和失败证据；通过定向测试验证最终入口与报告结论一致
- [ ] 6.2 实现与具体运行绑定的人工批准清单和确定性 Markdown/JSON 草稿导出，验证未批准、门禁失败、运行不匹配和正文被篡改的候选均无法导出，且已批准草稿仍保持 `PUBLIC_RESEARCH` 和未激活状态
- [ ] 6.3 由用户审核少量可批准候选后生成首份项目研究资料包，确认其中不含真实客群人数、库存、内部成本、兑换积分、项目转化率或收益；以中文提交最终保留代码、批准记录和资料包

## 7. 评审与项目事实同步

- [ ] 7.1 按 `CODE_REVIEW.MD` 复查来源真实性、提示注入、地址安全、数据边界、角色权限、失败可恢复性、比较公平性和过度抽象，只运行 `app/research` 及直接受影响的配置/模型测试并记录未执行的全量回归
- [ ] 7.2 完成 `docs/agent-design/63_公网研究建库与多Agent对照.md` 的真实结果与关键代码说明，更新 `20_Agent课程能力落地路线图.md` 的第八讲覆盖状态和动态调整记录，并追加任务日记；严格运行 OpenSpec 校验后以中文提交剩余文档与必要修正
