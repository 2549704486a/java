## 1. 数据事实源

- [x] 1.1 新增 Agent 请求与 Tool 观测表迁移、全新环境初始化 SQL、配置项和数据模型，并在 `docs/agent-design/62_Agent运行数据看板.md` 记录字段实例；执行迁移后验证唯一键、关联删除和敏感字段缺席，再以中文提交本步改动
- [ ] 1.2 实现 MySQL 幂等写入、30 天清理、汇总、趋势、分页列表和请求详情查询，使用固定数据验证空窗口、分子分母、最近秩 P95、Token 未知值及 Tool 双成功率后，以中文提交本步改动

## 2. 运行时采集

- [ ] 2.1 实现请求级观测上下文、模型用量累计器和有界后台写入队列，验证无模型调用、完整用量、部分缺失用量、队列满及存储异常不会修改 Agent 业务结果，并同步设计文档后以中文提交本步改动
- [ ] 2.2 将用户 Agent、运营 Agent 和现有 Tool 轨迹接入同一观测上下文，验证正常回答、运行异常、确定性无 Tool 回答及 Tool 业务拒绝产生符合口径的快照，并同步设计文档后以中文提交本步改动

## 3. 运营查询接口

- [ ] 3.1 新增 `agent:observe` 权限及汇总、请求列表、请求详情三个只读接口，验证无令牌、只有 `campaign:read`、具备专用权限、非法筛选和不存在请求等定向 API 用例，并同步设计文档后以中文提交本步改动

## 4. Agent 数据看板

- [ ] 4.1 在现有 `/operator` 工作台增加独立 Agent 数据视图、API 类型与客户端，完成窗口切换、手动刷新、指标卡、趋势、Tool 表和轨迹下钻，并通过 `npm run build` 及加载/空数据/失败状态的浏览器检查后，同步设计文档并以中文提交本步改动

## 5. 闭环验收

- [ ] 5.1 在本地启用观测并各执行一条用户 Agent 与运营 Agent 请求，核对数据库、三个 API 和页面展示的一致性；按 `CODE_REVIEW.MD` 检查隐私、权限、失败旁路和指标语义，只运行本模块及直接受影响用例，不执行无关全量回归
- [ ] 5.2 更新 `docs/agent-design/62_Agent运行数据看板.md` 的真实验收结果、课程能力路线图和当日日记，标明未验证范围与剩余风险；严格校验 OpenSpec 状态后以中文提交最终文档与必要修正
